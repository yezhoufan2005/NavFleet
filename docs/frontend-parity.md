# 前端功能等价基线（Phase 11A）

**这份文件是 Phase 14 的验收 checklist。** 新前端上线的条件不是"看起来更好"，而是这份清单里标了
「保留」「改造」的每一项都能核销。

盘点范围：12 个 SFC（2,537 行）+ 8 个 composable + store（761 行）+ 22 个 CSS partial（2,580 行），
逐文件读取，不靠记忆。所有条目都可回溯到 `file:line`。

## 怎么用

- `[ ]` → `[x]` 表示新前端已实现且经核对。
- **改造**项要在核销时写明"改成了什么、为什么"，不能悄悄降级。
- **废弃**项要在核销时确认"确实不需要"，而不是忘了做。
- 第 9 节是盘点中发现的**缺陷**：新前端要修掉，**照抄就是失职**。

## 图例

| 标记    | 含义                                                   |
| ------- | ------------------------------------------------------ |
| 🟢 保留 | 行为不变，只换实现与外观                               |
| 🟡 改造 | 保留能力但改变形态（IA / 交互 / 视觉），核销时说明改法 |
| 🔴 废弃 | 不再提供，核销时确认理由成立                           |
| ⚠️ 缺陷 | 现状是错的，新前端要修（详见第 9 节）                  |

## 0. 盘点纠正了三处此前的错误认知

写在最前面，因为 ROADMAP 的 11A 条目就是按错的认知写的：

1. **仪表盘没有筛选器、搜索框、排序控件。** 全文件零 `<input>`/`<select>`/`<label>`，唯一的筛选维度
   是编队选择，排序是硬编码的 deviceId 升序且用户不可改（`stores/fleet.ts:258-262`）。
   ROADMAP 11A 写的"每一个交互态、空态、快捷键"里，**快捷键一个都不存在**。
2. **没有告警抽屉。** `assets/styles/alert-drawer.css` 仍被 `main.css:11` 引入，但 `.alert-drawer` /
   `.drawer-scrim` 的标记在任何 `.vue` 里都不存在——**这是一批死 CSS**。ROADMAP 里"告警抽屉"一项作废。
3. **store 返回 28 个键，不是 30。** ROADMAP 11E 的数字错了，已在本文件第 8 节逐个列出。

另外两处小修正：两个地图组件的 a11y 属性全部来自基线提交，**不是** axe 补的（axe 那次改的是三个视图
和 CSS）；`RosSceneMap.vue:248-249` 有一条失效注释，说 `deviceExtentBounds` 供 `effectiveWorldBounds`
取景用，但后者从不引用它，真实消费者只有 `focusSelectedDevice`。

## 1. 应用外壳（`App.vue` 182 行）

### 1.1 三态顶层渲染

|       | 项                                         | 现状              | 去留 |
| ----- | ------------------------------------------ | ----------------- | ---- |
| `[ ]` | `status==='unknown'` → 加载屏「正在加载…」 | `App.vue:93-95`   | 🟢   |
| `[ ]` | `status==='anonymous'` → 登录页            | `App.vue:97-102`  | 🟢   |
| `[ ]` | `authenticated` → 外壳                     | `App.vue:104-179` | 🟢   |
| `[ ]` | toast 宿主在三态**之外**，登录页也能弹     | `App.vue:181`     | 🟢   |

### 1.2 header

|       | 项                                                                            | 现状                                  | 去留                         |
| ----- | ----------------------------------------------------------------------------- | ------------------------------------- | ---------------------------- |
| `[ ]` | skip-link「跳到主内容」，`top:-60px` → focus 时 `12px`（不用 `display:none`） | `App.vue:110`；`navigation.css:82-99` | 🟢                           |
| `[ ]` | 品牌块三行：`NavFleet` / `智能车队监控平台` / `fleetName \|\| "默认车队"`     | `App.vue:114-118`                     | 🟡 IA 重构后位置会变         |
| `[ ]` | 主导航 4 项（实时监控 / 历史回放 / 告警中心 / 设置），`aria-label="主导航"`   | `App.vue:120-128`                     | 🟡 分区扩展到 6-7 个         |
| `[ ]` | 告警中心的 `nav-badge`，仅 `summary.alertTotal` 为真时渲染                    | `App.vue:123-126`                     | 🟢                           |
| `[ ]` | 主题循环按钮，`aria-label` 带当前值，图标 `🌙/☀/🖥`                            | `App.vue:131-139`                     | 🟡 见 7.1                    |
| `[ ]` | 实时状态点，`data-online` 驱动色 + 2s `realtime-pulse` 动画，title 两态       | `App.vue:142-146`；`status.css:2-23`  | 🟢                           |
| `[ ]` | 会话芯片：用户名 + 角色中文（管理员/操作员/只读）                             | `App.vue:147-150`                     | 🟡 Phase 15 会加个人中心入口 |
| `[ ]` | 退出按钮                                                                      | `App.vue:151-153`                     | 🟢                           |

### 1.3 地标与焦点

|       | 项                                                                  | 现状                                       | 去留                                      |
| ----- | ------------------------------------------------------------------- | ------------------------------------------ | ----------------------------------------- |
| `[ ]` | 全站唯一 `<main id="main-content" tabindex="-1">`，视图内不得再嵌套 | `App.vue:174`；`DashboardView.vue:161-163` | 🟢 **这是 Phase 10 修过的回归，不能再犯** |
| `[ ]` | 路由变化后焦点转移到 `<main>`（首次挂载不触发）                     | `App.vue:30-35`                            | 🟢                                        |
| `[ ]` | `#main-content:focus` 去 outline、`:focus-visible` 给 2px brand 环  | `navigation.css:105-112`                   | 🟢                                        |

### 1.4 离线横幅与启动编排

|       | 项                                                                               | 现状                                | 去留       |
| ----- | -------------------------------------------------------------------------------- | ----------------------------------- | ---------- |
| `[ ]` | 横幅文案 + 重试按钮（`isRetrying` 重入保护，文案两态）                           | `App.vue:159-164`, `48-60`          | 🟢         |
| `[ ]` | ⚠️ 触发条件是 `!apiReady`，**冷启动期间必然可见**（初值 false，WS 断开不影响它） | `App.vue:47`；`stores/fleet.ts:135` | ⚠️ 见 9.11 |
| `[ ]` | `startDashboard()` 幂等闸门 → `registerWindowApi()` → `bootstrap()`              | `App.vue:62-69`                     | 🟢         |
| `[ ]` | 登出顺序：`disconnectRealtime()` → 复位闸门 → `auth.logout()`                    | `App.vue:78-82`                     | 🟡 见 9.12 |

## 2. 实时监控 / Dashboard（`DashboardView.vue` 487 行）

### 2.1 顶部统计卡（4 张）

|       | 项                                                                | 现状                                    | 去留                                       |
| ----- | ----------------------------------------------------------------- | --------------------------------------- | ------------------------------------------ |
| `[ ]` | 在线设备 `N / N`（`onlineCount / totalCount`，全量不受编队影响）  | `DV:139-143`；`stores/fleet.ts:289-298` | 🟢                                         |
| `[ ]` | 活动告警（`alerts.length` 求和，**全量不受编队筛选影响**）        | `DV:144-148`                            | 🟢                                         |
| `[ ]` | 当前编队（`formationName \|\| "全部设备"`）                       | `DV:149-153`                            | 🟡                                         |
| `[ ]` | 当前场景（场景名 → 裸 sceneId → `未配置场景` 三级降级）           | `DV:154-158`                            | 🟢                                         |
| `[ ]` | `aria-busy` 挂在区域上、骨架 `variant="value"` 与 `<strong>` 互斥 | `DV:138,141,146,151,156`                | 🟢                                         |
| `[ ]` | 骨架高度 27px = 20px×1.35 行盒，**实测 0px 位移**                 | `skeleton.css:29-38`                    | 🟢 **Phase 10 量过的细节，新前端要重新量** |
| `[ ]` | `prefers-reduced-motion` 下动画关闭改静态色块                     | `skeleton.css:59-69`                    | 🟢                                         |
| `[ ]` | 响应式 4 列 → ≤1560px 2 列 → ≤860px 1 列                          | `responsive.css:19-21,68-75`            | 🟡 断点体系重做                            |

### 2.2 左栏折叠

|       | 项                                                                      | 现状             | 去留 |
| ----- | ----------------------------------------------------------------------- | ---------------- | ---- |
| `[ ]` | 折叠按钮 `»`/`«`，`aria-label`+`title` 同步两态                         | `DV:169-177`     | 🟢   |
| `[ ]` | 折叠态持久化 `localStorage["navfleet:fleet-collapsed"]`，值 `"1"`/`"0"` | `DV:48-64`       | 🟢   |
| `[ ]` | 标题与计数用 `v-show`（留在 DOM），编队+列表用 `v-if`（移出 DOM）       | `DV:167-168,180` | 🟢   |
| `[ ]` | 计数是**筛选后**数量（`filteredDevices.length`），不是总数              | `DV:168`         | 🟢   |

### 2.3 编队区

|       | 项                                                                                    | 现状                                    | 去留                        |
| ----- | ------------------------------------------------------------------------------------- | --------------------------------------- | --------------------------- |
| `[ ]` | 「全部设备」按钮，`aria-pressed`，清空编队后重算选中设备                              | `DV:190-198`；`stores/fleet.ts:476-479` | 🟢                          |
| `[ ]` | 编队卡三要素：名称 / `onlineCount/deviceCount` / 颜色（仅 color 非空才注入 CSS 变量） | `DV:210-216`                            | 🟢                          |
| `[ ]` | 选中态 `active` 类 + `aria-pressed` 同表达式                                          | `DV:208-209`                            | 🟢 **Phase 10 的 axe 修复** |
| `[ ]` | 计数由 store 重算（成员实数优先），场景可由成员唯一场景推导                           | `stores/fleet.ts:219-238`               | 🟢                          |
| `[ ]` | 排序按 `formationId`（**不是名称**）localeCompare                                     | `stores/fleet.ts:264-268`               | 🟡 建议改为按名称           |
| `[ ]` | ⚠️ 点击编队会**强制切到 scene 模式但不落盘**，与 `setMapMode` 不一致                  | `stores/fleet.ts:467-474`               | ⚠️ 见 9.6                   |
| `[ ]` | `formation.description`（3 条配置文案）**零展示**                                     | `configRegistry` 下发                   | 🟡 新前端接上               |
| `[ ]` | 空态「当前没有编队配置」（`empty-alert.compact` 虚线框）                              | `DV:219-221`                            | 🟢                          |

### 2.4 设备列表

|       | 项                                                                                                          | 现状                            | 去留                                  |
| ----- | ----------------------------------------------------------------------------------------------------------- | ------------------------------- | ------------------------------------- |
| `[ ]` | 行内 5 项：设备名 / 设备 ID（**仅重名时显示**）/ 状态徽标 / 最近上报 / 电量                                 | `DV:239-257`                    | 🟢                                    |
| `[ ]` | 重名判定基于**全量** `sortedDevices`，所以编队外重名也会让本行显示 ID                                       | `DV:70-82,114-116`              | 🟢                                    |
| `[ ]` | tone 优先级：`offline → critical → warning → notice → normal`                                               | `stores/fleet.ts:240-254`       | 🟢                                    |
| `[ ]` | tone → 中文：正常/预警/告警/提示/离线（**同一张表在 GpsMap 里复制了一份**）                                 | `DV:36-42`；`GpsMap.vue:20-26`  | 🟡 抽成共享                           |
| `[ ]` | 行级视觉：critical/warning 换投影色，offline 整行 `opacity:.74`                                             | `device-list.css:35-45`         | 🟢                                    |
| `[ ]` | 选中态 `selected` 类 + `aria-current`                                                                       | `DV:233-234`                    | 🟢                                    |
| `[ ]` | 点击 `selectDevice(id, {preserveFormation: Boolean(selectedFormation)})` —— 从列表点选**永不**清空编队      | `DV:131-133`                    | 🟢                                    |
| `[ ]` | 排序固定 deviceId 升序，代码注释说明是为了行不跳动                                                          | `stores/fleet.ts:258-262`       | 🟡 新前端要提供排序但**默认必须稳定** |
| `[ ]` | 🔴 无筛选器 / 搜索 / 分页 / 虚拟滚动                                                                        | 见第 0 节                       | 🟡 新前端要加（`tags` 是现成维度）    |
| `[ ]` | 三态互斥：骨架 4 卡 / 空态「当前筛选条件下没有设备数据」/ 设备行                                            | `DV:226,261-263`                | 🟢                                    |
| `[ ]` | 虚拟化**实测后决定不做**：6/50/200/500 台的挂载与更新耗时已入库，断言放在每行 DOM 节点数（当前 8，上限 16） | `test/views/largeFleet.test.ts` | 🟢 判据沿用，别凭感觉重开             |

### 2.5 地图区

|       | 项                                                                               | 现状                              | 去留                      |
| ----- | -------------------------------------------------------------------------------- | --------------------------------- | ------------------------- |
| `[ ]` | GPS / ROS 两个 tab，`aria-pressed`（标签是 "ROS"，内部值是 `"scene"`）           | `DV:272-289`                      | 🟢                        |
| `[ ]` | 模式持久化 `localStorage["navfleet:map-mode"]`，白名单校验，非法回落 `gps`       | `stores/fleet.ts:106-117,481-489` | 🟢                        |
| `[ ]` | 「清除轨迹」按钮，仅 scene 模式且轨迹点 `> 1` 时渲染                             | `DV:290-298`                      | 🟢                        |
| `[ ]` | GpsMap 收**全量** `sortedDevices`；RosSceneMap 收**编队筛选后**的 `sceneDevices` | `DV:305,315`                      | 🟢 这个不对称是有意的     |
| `[ ]` | 点 GPS 标记后：若该设备有 sceneId 则**自动切到 scene 模式并落盘**                | `DV:118-125`                      | 🟡 体验上有争议，重新评估 |
| `[ ]` | ROS 不可用空态「暂无 ROS 地图 / 当前选中设备或编队还没有可用的场景地图配置。」   | `DV:321-324`                      | 🟢                        |
| `[ ]` | 面板头只有标题 + tab 组，**无设备数/场景名/时间戳**                              | `DV:269-300`                      | 🟡 新前端可加             |

### 2.6 右侧详情面板 —— 逐字段

面板固定 5 个 section，**全部展开、无折叠、无 tab、无锚点**（`DV:346-477`）。头部与滚动区是两个独立
`v-if`，条件相同。

头部：`deviceName` / `deviceId`（**这里始终显示**，与列表行不同）/ 编队标签（仅选中编队时）/ 状态药丸。

`div.detail-scroll` 带 **`tabindex="0"`**（`DV:345`）—— 可滚动且内部无可聚焦元素，不加键盘用户取不到
折叠线以下内容。**这是 axe `scrollable-region-focusable` 抓出来的，新前端结构变了也要保证等效可达。**

**① 车辆信息**（2 列 → ≤1320px 1 列）

|       | 标签     | 取值                      | 格式化                              | 缺失表现                |
| ----- | -------- | ------------------------- | ----------------------------------- | ----------------------- |
| `[ ]` | 控制模式 | `vehicleInfo.controlMode` | `formatEnum` + `title=describeEnum` | `--`（title 省略）      |
| `[ ]` | 档位     | `vehicleInfo.gear`        | 同上（`-1 R / 0 N / 1 D / 2 P`）    | `--`                    |
| `[ ]` | 速度     | `vehicleInfo.speed`       | `formatNumber(v,2," m/s")`          | ⚠️ `null → 0.00 m/s`    |
| `[ ]` | 角速度   | `vehicleInfo.omega`       | `formatNumber(v,3," rad/s")`        | ⚠️ `null → 0.000 rad/s` |
| `[ ]` | 电量     | `vehicleInfo.soc`         | `formatNumber(v,1,"%")`             | ⚠️ `null → 0.0%`        |

枚举字典共 13 条（`utils/enums.ts`）：`controlModeMap` 4 条（待命/人工、自动驾驶、遥控接管、紧急停止）、
`gearMap` 4 条、`taskStatusMap` 5 条（空闲/执行中/已完成/异常中断/充电中）。每条都有 description 走 `title`。

**② 设备报码**（3 张卡，**恒定渲染**即使 code 全为 0，无空态）

|       | 卡片     | tone     | 卡内 4 处输出                                                                                              |
| ----- | -------- | -------- | ---------------------------------------------------------------------------------------------------------- |
| `[ ]` | 提示报码 | notice   | 标题 / 码徽标（`formatValue`，`0` 显示 `"0"` 而非 `--`）/ 内容（空串 → `暂无内容`）/ 时间（`formatStamp`） |
| `[ ]` | 预警报码 | warning  | 同上                                                                                                       |
| `[ ]` | 告警报码 | critical | 同上                                                                                                       |

**③ 任务信息**

|       | 标签         | 取值                 | 格式化                              |
| ----- | ------------ | -------------------- | ----------------------------------- |
| `[ ]` | 任务状态     | `taskStatus`         | `formatEnum(taskStatusMap)` + title |
| `[ ]` | 平台任务状态 | `platformTaskStatus` | 同上（复用同一张表）                |
| `[ ]` | 上报时间     | `stamp`              | `formatStamp`，`.wide` 跨 2 列      |

**④ ROS 位姿**（2 张对称卡）

|       | 项                                                                                         | 现状 |
| ----- | ------------------------------------------------------------------------------------------ | ---- |
| `[ ]` | 融合定位卡：状态徽标 `hasPose ? "已定位" : "无数据"` + `x：/y：/yaw：`（全角冒号，无单位） |
| `[ ]` | 激光定位卡：同构                                                                           |
| `[ ]` | `hasPose` 只看 x/y **不看 yaw**；归一化阶段会把 lidar 拷进缺失的 fusion                    |
| `[ ]` | ⚠️ 徽标说"无数据"但数值行显示 `0.00`（见 9.1）                                             |
| `[ ]` | 分节空态「当前设备还没有可用的 ROS 位姿数据。」**追加在两张卡下方，卡不隐藏**              |

**⑤ 限速信息**

|       | 标签     | 取值                      | 格式化                          | 缺失表现         |
| ----- | -------- | ------------------------- | ------------------------------- | ---------------- |
| `[ ]` | 限速值   | `speedLimit.limit`        | `formatNumber(v,2)`，**无单位** | ⚠️ `null → 0.00` |
| `[ ]` | 减速时长 | `speedLimit.slowdownTime` | `formatValue`，**无单位**       | `--`             |
| `[ ]` | 来源模块 | `speedLimit.moduleName`   | `formatValue`                   | `--`             |
| `[ ]` | 更新时间 | `speedLimit.stamp`        | `formatStamp`，`.wide`          | `--`             |

⚠️ 同一张表里"限速值"用 `formatNumber`（缺失变 0.00）而"减速时长"用 `formatValue`（缺失变 `--`）——
**口径不一致，新前端统一**。

**面板空态**：「暂无设备详情 / 请先从左侧选择一台设备或一个编队。」实际只在零设备时出现（`ensureSelectedDevice`
每次 ingest 后都会自动选中第一台）。

**防御性缺口**：模板对嵌套对象**不做可选链**（`selectedDevice.vehicleInfo.controlMode`、`item.payload.code`、
`fusionLoc.x`、`speedLimit.limit`），完全依赖 `createDefaultDevice` 保证子对象存在。新前端若直接吃原始
payload 会抛错 —— 归一化不可跳过。

### 2.7 交互态与无障碍

|       | 项                                                                                                                 | 现状                     | 去留                |
| ----- | ------------------------------------------------------------------------------------------------------------------ | ------------------------ | ------------------- |
| `[ ]` | 6 类可交互元素全部是原生 `<button type="button">`，天然可 Tab / Enter / Space                                      | `DV` 全文                | 🟢 无头库能免费拿到 |
| `[ ]` | Tab 顺序 = DOM 顺序：折叠钮 → 全部设备 → 编队 → 设备行 → GPS → ROS → 清除轨迹 → 详情滚动区                         | —                        | 🟢                  |
| `[ ]` | hover 统一 `translateY(-1px)` + 0.18s 过渡                                                                         | `controls.css:1-21`      | 🟡                  |
| `[ ]` | ⚠️ **本页所有控件都没有自定义 focus 样式**（全站只有 skip-link / 登录框 / 设置页有）                               | `grep focus` 于 CSS 目录 | ⚠️ 见 9.9           |
| `[ ]` | 🔴 无 `:active` 规则、无 disabled、无快捷键、无方向键导航、无 roving tabindex、无 `role="tablist"`、无 `aria-live` | —                        | 🟡 新前端补齐       |

## 3. ROS 场景地图（`RosSceneMap.vue` 594 + `useSvgViewport.ts` 706）

> **这一节是新前端最大的技术风险。** `useSvgViewport` 是 v1.0.0 里花三次尝试才定位到根因的文件，
> 且覆盖率只有 **1.07%**。Phase 13A 的决定是**原样搬、不重写**——下面的条目因此主要是"搬过去之后
> 要验证仍然成立"，而不是"照着重新实现"。

### 3.1 坐标系与变换

|       | 项                                                                                                        | 现状 |
| ----- | --------------------------------------------------------------------------------------------------------- | ---- |
| `[ ]` | `viewBox="0 0 (w\|\|1000) (h\|\|620)"`，SVG CSS 尺寸 100% → 1 viewBox 单位 = 1 CSS px                     |
| `[ ]` | `stageTransform` = `translate(offset) scale(s, -s) translate(-minX, -maxY)`，**Y 轴翻转**，锚点世界左上角 |
| `[ ]` | 视口状态就 5 个数：`{width, height, scale, offsetX, offsetY}`，初值 `1000/620/1/0/0`                      |
| `[ ]` | 屏→世界与世界→屏两个互逆函数，坐标 3 位小数，**越界返回 null**                                            |
| `[ ]` | 面板尺寸来自 `getBoundingClientRect()` + `ResizeObserver`，卸载时 disconnect                              |

### 3.2 平移

|       | 项                                                                | 现状                        | 去留                      |
| ----- | ----------------------------------------------------------------- | --------------------------- | ------------------------- |
| `[ ]` | 5 个 pointer 事件（`pointercancel`/`pointerleave` 都路由到 up）   | `RosSceneMap.vue:368-372`   | 🟢                        |
| `[ ]` | 仅 `button === 0` 触发（无中键/右键拖拽），用 `setPointerCapture` | `useSvgViewport.ts:545-559` | 🟢                        |
| `[ ]` | **无任何边界约束** —— 世界可被拖到完全出屏                        | `:561-574`                  | 🟡 新前端建议加软边界     |
| `[ ]` | `CLICK_THRESHOLD_PX = 6`，只有超过才算拖动并落盘                  | `:27,568,586-588`           | 🟢                        |
| `[ ]` | `dragging` → `cursor: grab/grabbing`，`touch-action: none`        | `map-overlays.css:8-15`     | 🟢                        |
| `[ ]` | 🔴 单指针：无双指缩放、无惯性                                     | —                           | 🟡 平板是真实场景，建议补 |

### 3.3 缩放

|       | 项                                                                                                                         | 现状                    | 去留                   |
| ----- | -------------------------------------------------------------------------------------------------------------------------- | ----------------------- | ---------------------- |
| `[ ]` | **只有滚轮**，无缩放按钮、无双击                                                                                           | `RosSceneMap.vue:367`   | 🟡 新前端补按钮 + 键盘 |
| `[ ]` | 固定步长 `×1.12` / `×0.88`，不随 `deltaY` 比例变化                                                                         | `useSvgViewport.ts:529` | 🟢                     |
| `[ ]` | 缩放中心 = 光标，且**光标在世界范围外时滚轮无效**                                                                          | `:524-541`              | 🟢                     |
| `[ ]` | min/max **相对于适应缩放**：`base = min(w/W, h/H) × 0.92`，`min = base × (minZoom ?? 0.75)`，`max = base × (maxZoom ?? 8)` | `:133-154`              | 🟢 关键：不是绝对值    |
| `[ ]` | `minZoom/maxZoom` 来自**合并后**的场景（scenes.json ∪ metadataUrl JSON）                                                   | `RosSceneMap.vue:30-66` | 🟢                     |
| `[ ]` | HUD 显示 `round(scale,2)+"x"`（裸 scale，不是 base 的倍数）                                                                | `RosSceneMap.vue:349`   | 🟡 显示倍数更可读      |
| `[ ]` | ⚠️ 每个滚轮刻度一次同步 sessionStorage 读写 + 两次 `getBoundingClientRect`                                                 | `:536,542`              | ⚠️ 见 9.3              |

### 3.4 「适应场景」与「定位车辆」—— 两个算法必须分清

|       | 项                                                                                                                                                                                                 | 现状 |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---- |
| `[ ]` | **适应场景** `resetView`：`scale = clamp(base × (defaultView.zoom ?? 1))`；有 `defaultView.center` 则居中它，否则 `fitWorldBounds` 居中整个 extent（留 8% 余量）                                   |
| `[ ]` | **定位车辆** `focusSelectedDevice`：返回 `boolean`，让调用方能选择不动视口                                                                                                                         |
| `[ ]` | 定位的位姿优先级：`fusionLoc` → `lidarLoc` → null                                                                                                                                                  |
| `[ ]` | **无位姿 + 有僚车** → 取景整个编队 extent；**无位姿 + 无僚车** → 返回 false，视口不动                                                                                                              |
| `[ ]` | **有位姿 + 有僚车** → `fitToRegion(regionCenteredOn(pose, extent))`                                                                                                                                |
| `[ ]` | **`regionCenteredOn` 的意图**：直接 fit extent 会把边缘车辆推到面板边（实测 637px 面板里偏 262px）。把 extent 关于车辆位置镜像成对称框，既保证车在正中，又保证僚车全在屏内，代价是视野比紧包围盒宽 |
| `[ ]` | `fitToRegion` 留 18% padding（`× 0.82`）                                                                                                                                                           |
| `[ ]` | **有位姿 + 无僚车** → 固定窗口 `clamp(max(base × 1.3, viewport.width / 45))`，约 45 世界米铺满面板宽                                                                                               |
| `[ ]` | 历史坑：`resetView` 曾在有位姿时结束于 45m 特写（1000px 面板上 ≈22.22x，只覆盖 74×88m 路网的 1/6）。**e2e 用「点适应场景后车辆偏离中心 >24px」把这个语义钉住了**                                   |

### 3.5 视图记忆

|       | 项                                                                                                                          | 现状 |
| ----- | --------------------------------------------------------------------------------------------------------------------------- | ---- |
| `[ ]` | **sessionStorage**（非 local），key `navfleet:ros-scene-views:v2`，按 tab 生命周期 —— 几天前的陈旧视图比重新适应场景更困扰  |
| `[ ]` | 形状 `{ [sceneId]: {centerX, centerY, scale, updatedAt} }`，中心 4 位、缩放 6 位小数                                        |
| `[ ]` | `:v2` 是迁移手段：恢复路径只校验**中心越界**、不校验离谱**缩放**，所以靠升 key 淘汰历史 22.22x 条目                         |
| `[ ]` | **7 个写入点**：滚轮 / 拖动释放（仅超阈值）/ 适应场景 ×2 / 定位车辆 ×3 / 恢复成功后回写 / resize 同步 / bounds watcher      |
| `[ ]` | 恢复校验：条目存在 + 中心在 `effectiveWorldBounds` 内 + scale 有限，且 scale 按当前面板重新 clamp                           |
| `[ ]` | 恢复优先级：`restore → focusSelectedDevice → resetView`                                                                     |
| `[ ]` | 开屏落在**已定位的车辆**而非场景适应，是有意的（场景适应曾让车偏离中心 276px、距边 59px，操作员每次进来都要点一次定位车辆） |
| `[ ]` | 跨刷新（同 tab）保留，e2e 断言刷新后车辆仍 `<24px` 偏移；新 tab 从空开始                                                    |
| `[ ]` | 跨场景切换：`activeSceneId` watcher 清 `hydratedSceneId` 触发重新 hydrate，各场景条目互不影响                               |
| `[ ]` | 设置页显示已保存场景数并可一键清除                                                                                          |

### 3.6 `hasMeasuredPanel` —— hydration 门禁（**最容易被重写掉的一行**）

|       | 项                                                                                                                                                                                                                                                                                                                                                              | 现状 |
| ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---- |
| `[ ]` | 模块局部 `let hasMeasuredPanel = false`，只在 `updateViewportSize` 读到非零 rect 后翻真，**从不复位**                                                                                                                                                                                                                                                           |
| `[ ]` | `applyStoredOrDefaultView` 在 `sceneId && sceneReady && hasMeasuredPanel` 三者齐备前直接 return                                                                                                                                                                                                                                                                 |
| `[ ]` | **它修的缺陷**：bounds watcher 带 `immediate: true`，在 setup 阶段就跑，早于 `onMounted` 测量面板，于是首屏视图按 1000×620 占位尺寸算完，`updateViewportSize` 随后只改 width/height **不动 offset**，静默作废它；ResizeObserver 再从这个自相矛盾的状态推出"上一个中心点"并忠实保住那个错的点。实测：为 1000×620 居中的车辆，在真实 637×802 面板里偏离 **274px** |
| `[ ]` | 修复后的顺序：setup watcher 跳过 → `onMounted` 测量 → hydrate → 挂 ResizeObserver                                                                                                                                                                                                                                                                               |
| `[ ]` | 推论：0×0 的隐藏面板会一直停在占位尺寸，直到拿到真实测量                                                                                                                                                                                                                                                                                                        |

### 3.7 `rebaseOffsetsToBounds` —— 另一个不可省的补偿

|       | 项                                                                                                                                                                                                                         | 现状 |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---- |
| `[ ]` | `offsetX += (minX - prevMinX) × scale`；`offsetY -= (maxY - prevMaxY) × scale`                                                                                                                                             |
| `[ ]` | **意图**：`stageTransform` 锚在 `minX/maxY`，lanelet overlay 落地会加宽 `effectiveWorldBounds`，整个视图随之滑动，而 watcher 接着把滑走的结果**存了下来**。实测 637px 面板里漂移 **199px**，读起来就是"地图从来没定位到车" |
| `[ ]` | bounds watcher 四个分支顺序：同场景已 hydrate → rebase；未 hydrate → hydrate 并返回；中心缺失/越界 → hydrate 并返回；否则落盘                                                                                              |
| `[ ]` | resize 同步：**保留缩放、不重新取景**；旧中心仍在界内则重新居中它                                                                                                                                                          |
| `[ ]` | 选中变化 watcher：同场景内换车调 `focusSelectedDevice()`，新车无位姿则回落 `resetView()`                                                                                                                                   |
| `[ ]` | 选中车的遥测移动**不移动视图**（世界空间是静态地图 extent），地图因此不抖                                                                                                                                                  |

### 3.8 三类底图

|       | 项                                                                                                                                              | 现状                                   | 去留      |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------- | --------- |
| `[ ]` | 世界范围仲裁：lanelet 有 bounds 且无背景层 → **只用 lanelet bounds**（忽略配置 bounds）；否则三者取并集                                         | `RosSceneMap.vue:155-169`              | 🟢        |
| `[ ]` | `hasBounds` 要求 4 个有限数**且** `maxX>minX && maxY>minY`，退化框视为不存在                                                                    | `:22-28`                               | 🟢        |
| `[ ]` | **栅格 SVG**：需 `imageUrl` + 5 个有限数；`transform` 反向翻转抵消 stage 的 Y 翻转，`preserveAspectRatio="none"`，`image-rendering="pixelated"` | `:80-107,390-400`                      | 🟢        |
| `[ ]` | ⚠️ 栅格图 HTTP 失败**完全静默**（破图，无错误 UI、无 toast）                                                                                    | `:390-400`                             | ⚠️ 见 9.4 |
| `[ ]` | **点云**：PCD 二进制 → 离屏 canvas 栅格化 → PNG dataURL → 塞回 SVG `<image>`                                                                    | `point-cloud.ts:292-337`               | 🟢        |
| `[ ]` | PCD 只支持 `DATA binary`（前 64KiB 内找头），否则抛「暂不支持当前 PCD 格式…」                                                                   | `:46,72-127`                           | 🟢        |
| `[ ]` | z 分带分类：obstacle → 2、floor → 1、**两带都没配 → 全算 floor**；obstacle 每格胜过 floor                                                       | `:208-237,280-283`                     | 🟢        |
| `[ ]` | 像素配色：obstacle `rgb(182,237,255)` alpha≥164；floor `rgb(108,132,148)` alpha 64-146；class 0 全透明。**行序翻转**以对齐 Y 翻转               | `:305-329`                             | 🟢        |
| `[ ]` | 缓存：模块级 `Map<key, Promise>`，key 含 url+几何参数；**缓存 promise**（并发去重），失败删 key 可重试，成功**永不淘汰**                        | `:44,347-374`                          | 🟢        |
| `[ ]` | 竞态守卫：单调 `pointCloudRequestId`，过期结果丢弃，卸载时递增以放弃在途                                                                        | `useSceneOverlay.ts:38,83-101,141-143` | 🟢        |
| `[ ]` | 点云失败 → 常驻 `.ros-warning` 卡「点云背景加载失败」+ 原因，**与空态不互斥**                                                                   | `RosSceneMap.vue:579-582`              | 🟢        |
| `[ ]` | 🔴 点云**无加载指示**（加载中退回栅格图或无底图）                                                                                               | —                                      | 🟡 补     |
| `[ ]` | **Lanelet2**：前端不读 `osmUrl`，后端转成 `overlayUrl` JSON；失败 → overlay=null + toast「路网覆盖层加载失败，地图将不显示车道线。」            | `useSceneOverlay.ts:40-58`             | 🟢        |
| `[ ]` | ⚠️ overlay/metadata **无 request-id 守卫**（点云有），慢请求可能跨场景落地                                                                      | `:40-80`                               | ⚠️ 见 9.5 |
| `[ ]` | metadata 失败 → toast「场景元数据加载失败，地图可能无法正确定位。」                                                                             | `:61-80`                               | 🟢        |
| `[ ]` | 🔴 overlay / metadata 均**无加载态**                                                                                                            | —                                      | 🟡 补     |

### 3.9 叠加层要素（绘制顺序 = DOM 顺序）

|       | 要素                                  | 编码要点                                                                                                |
| ----- | ------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| `[ ]` | 画布底色 rect                         | `--ros-canvas`，`!sceneReady` 时也画                                                                    |
| `[ ]` | 世界底色 rect                         | `--ros-free`；有 overlay 且无背景层时切 `--ros-lanelet-bg`                                              |
| `[ ]` | extent 描边                           | ⚠️ 硬编码 `rgba(15,28,39,0.56)`，**未主题化**                                                           |
| `[ ]` | lanelet 左右边界                      | `--ros-lanelet-line`，2.4 宽，round cap，`non-scaling-stroke`；左右各一趟 `v-for`                       |
| `[ ]` | lanelet 中心线                        | `--ros-lanelet-center`，1.3 宽，虚线 `10 8`                                                             |
| `[ ]` | 融合定位标记                          | 环 r=24（`pulse 2.2s` 动画）+ 核心圆 r=10（brand 描边 3）+ 箭头，`rotate(90 - deg(yaw))`                |
| `[ ]` | 激光定位标记                          | 环 r=20 + **16×16 rect 旋转 45°**（菱形，warning 描边）+ 箭头 0.92 缩放                                 |
| `[ ]` | 朝向算法                              | `angle = 90 - round(yaw × 180/π, 1)`，yaw 非有限则 0。把 ROS 的"从 +X 轴逆时针弧度"换成 SVG 旋转        |
| `[ ]` | 屏幕不变尺寸                          | `1/max(scale, 1e-4)` 逆缩放，保持标记像素尺寸恒定**并把文字翻正**                                       |
| `[ ]` | 僚车标记                              | 圆 r=7，默认白底深描边；`data-tone` 只覆盖 critical/warning/notice（normal/offline 保持白）             |
| `[ ]` | 僚车标签                              | **始终显示**（与 GPS 的 hover-only 不同），9px 粗体 + `paint-order: stroke` 描边光晕                    |
| `[ ]` | 选中态                                | 🔴 ROS 图**没有**逐车选中样式；选中车靠"哪些标记形状存在"表达 + 脉冲环                                  |
| `[ ]` | fusion↔lidar 连线                     | `--ros-link`，2 宽，虚线 `8 6`，两点都存在时才画                                                        |
| `[ ]` | 选中轨迹                              | `rgba(96,197,255,.85)`，2.4 宽，虚线 `2 5`，带 drop-shadow                                              |
| `[ ]` | 僚车轨迹                              | `rgba(148,176,214,.4)`，1.6 宽，虚线 `2 6`；`data-tone` 只覆盖 critical/warning                         |
| `[ ]` | 轨迹上游                              | 实时：`TRAIL_MIN_DISTANCE = 0.12m` 去抖 + `TRAIL_MAX_POINTS = 240` 截断；回放：窗口起点→cursor 全量重建 |
| `[ ]` | ⚠️ 路径构建                           | 非有限点被丢弃但**其余仍拼接**，缺口被静默直连；首点被丢则路径以 `L` 开头                               |
| `[ ]` | HUD                                   | 两卡：场景名 / `Nx` 缩放                                                                                |
| `[ ]` | 图例                                  | 路网覆盖（有 overlay 时）/ 点云背景（**含加载中与失败后**）/ 融合定位、激光定位（**无条件**）           |
| `[ ]` | 工具栏                                | 仅两个按钮：适应场景、定位车辆                                                                          |
| `[ ]` | 🔴 **无坐标网格、无比例尺、无坐标轴** | 唯一空间线索是 extent 描边 + HUD 数字 → 🟡 新前端建议补比例尺                                           |

### 3.10 ROS 图空态

|       | 分支                                                                               | 触发                     | 文案                                              |
| ----- | ---------------------------------------------------------------------------------- | ------------------------ | ------------------------------------------------- |
| `[ ]` | 警告（独立，可与下面并存）                                                         | `pointCloudError`        | 点云背景加载失败 + 原因                           |
| `[ ]` | 空态 1                                                                             | `!sceneReady`            | 暂无可用地图 / 当前场景缺少有效的 ROS 地图元数据… |
| `[ ]` | 空态 2                                                                             | 有场景但无融合也无激光点 | 暂无 ROS 位姿 / …地图仍可用于查看当前场景。       |
| `[ ]` | ⚠️ 两个空态是 `inset:0` 且**无 `pointer-events:none`**，显示时会吞掉滚轮与指针事件 |                          | ⚠️ 见 9.7                                         |

## 4. GPS 地图（`GpsMap.vue` 311 + `utils/amap.ts` 91）

|       | 项                                                                                                                                                 | 现状                                  | 去留                                |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------- | ----------------------------------- |
| `[ ]` | 需要 **两个** env（`VITE_AMAP_KEY` + `VITE_AMAP_SECURITY_JS_CODE`），缺任一即不发 SDK 请求                                                         | `amap.ts:10-16`；`GpsMap.vue:202-205` | 🟢                                  |
| `[ ]` | 四条明确错误文案（缺 key / 缺安全码 / 脚本 onerror / 加载了但无全局对象）                                                                          | `amap.ts:18-26,57,72-82`              | 🟢                                  |
| `[ ]` | 模块级 promise 去重；失败置空以便下次重试                                                                                                          | `:29-48,85-90`                        | 🟢                                  |
| `[ ]` | ⚠️ 已存在脚本分支：若该脚本**已加载完**却没暴露 `window.AMap`，两个事件都不会再触发，promise 永不 settle                                           | `:50-63`                              | ⚠️ 见 9.8                           |
| `[ ]` | 初始化：2D、zoom 11、中心 `[121.4737, 31.2304]`、`zooms:[3,20]`、`resizeEnable`                                                                    | `GpsMap.vue:216-223`                  | 🟢                                  |
| `[ ]` | 主题：`whitesmoke` / `darkblue`，构造时给 + watcher `setMapStyle`                                                                                  | `:16-18,42-46`                        | 🟢                                  |
| `[ ]` | 设备过滤：`gpsEnabled !== false` **且** lat/lng 均有限                                                                                             | `:48-55`                              | 🟢                                  |
| `[ ]` | 坐标转换 WGS84 → GCJ02，非有限返回 null（跳过该 marker）                                                                                           | `utils/gps.ts:41-58`                  | 🟢                                  |
| `[ ]` | marker 复用 + 差分：先删除消失的；存在的总是 `setPosition`，**内容仅在 signature `tone\|selected\|name\|id\|heading` 变化时重建**                  | `:139-190`                            | 🟢                                  |
| `[ ]` | 上层 `markerSignature` 非 deep watch（替代了 `{deep:true}`）：只动 `soc` 的一 tick 不重绘                                                          | `:255-278`                            | 🟢 **有决策注释，务必保留这个思路** |
| `[ ]` | 朝向箭头：`--heading` CSS 变量，46×46 容器绕 pin 中心旋转，0 = 正北顺时针                                                                          | `map-overlays.css:99-129`             | 🟢                                  |
| `[ ]` | marker 根盒 18×18 **且不带 CSS transform** —— 旧的 `translate(-50%,-100%)` 是像素偏移，zoom 16 约 170m、zoom 11 约 5km，这就是"车随缩放漂移"的成因 | `map-overlays.css:67-115`             | 🟢 **重写时最容易复发的坑**         |
| `[ ]` | 标签：`deviceName` + `tone 中文 / deviceId`，**hover 或选中才显示**                                                                                | `:81-97`                              | 🟡                                  |
| `[ ]` | tone 编码：`color` 同时驱动 pin 环与朝向箭头                                                                                                       | `map-overlays.css:194-234`            | 🟢                                  |
| `[ ]` | 选中态：`pulse` 动画 + 标签强制可见 + zIndex 160（vs 110）                                                                                         | `:164-242`                            | 🟢                                  |
| `[ ]` | **首次取景只做一次**（`hasFittedOnce`）：之后视口归用户。原因是演示车队有车会掉线抖动，每次设备集变化都 fit 会在用户平移途中把地图拽走             | `:36-39,193-195`                      | 🟢                                  |
| `[ ]` | `fitFleet`：单车 `setZoomAndCenter(16,…,300ms)`；多车 `setFitView(padding [64,72,64,72], maxZoom 16)`                                              | `:115-128`                            | 🟢                                  |
| `[ ]` | `focusSelected`：`max(currentZoom, 16)`，**永不缩小**                                                                                              | `:130-137`                            | 🟢                                  |
| `[ ]` | AMap 内置 `Scale` + `ToolBar`（右下），应用自己的「适应车队」按钮在右上避开它                                                                      | `:226-234`                            | 🟡                                  |
| `[ ]` | 三个互斥分支：占位（无配置/加载失败）/ 加载中覆盖层 / 无 GPS 数据覆盖层                                                                            | `:283-308`                            | 🟢                                  |
| `[ ]` | 卸载：`destroyMarkers()` → `map.destroy()`，refs 置 null                                                                                           | `:104-113,248-253`                    | 🟢                                  |
| `[ ]` | ⚠️ `aria-label` 挂在**无 role 的 div** 上，不可靠暴露                                                                                              | `:290`                                | ⚠️ 见 9.10                          |

## 5. 历史回放（`HistoryView.vue` 338 + `useHistoryPlayback.ts` 123）

**核销（Phase 13E）：整节的形态变了一次 —— 历史回放不再是页面，而是设备详情的一个 tab。**
理由写在 `docs/frontend-ia.md`：独立页的代价是查完实时还要**重新选一次设备**，那正是 11B 那条
"6 步且拿不到答案"排故流程的一半。设备由路由决定之后，这一节里所有"选设备"相关的条目自然废弃，
剩下要选的只有时间窗。tab 名进 `?tab=playback`，所以一次回放是一个可以发出去的链接。

### 5.1 查询区

|       | 项                                                                                                                   | 现状                           | 去留            |
| ----- | -------------------------------------------------------------------------------------------------------------------- | ------------------------------ | --------------- |
| `[x]` | 设备下拉，默认取 `sortedDevices[0]`（**仅当当前为空**，所以车队刷新不覆盖用户选择）；无设备时唯一选项「暂无设备」    | `:38-53,170-178`               | 🔴 见下         |
| `[x]` | 起止时间 `datetime-local` 两个，**无先后校验**（允许 from > to）                                                     | `:179-186`                     | 🟡 已加校验     |
| `[x]` | 最大点数 `number`，默认 1000                                                                                         | `:187-190`                     | 🔴 见下         |
| `[x]` | ⚠️ `min="1" max="5000"` 因无 `<form>` 而**不产生任何约束**；实际清洗只有 `Number(v) \|\| 1000`                       | `:139`                         | ⚠️ 随控件一并删 |
| `[x]` | 快捷范围 3 个（1h / 6h / 24h）+ 清除时间；**只改输入框，不触发查询**                                                 | `:120-129,196-209`             | 🟡 已直接查询   |
| `[x]` | 🔴 **无 `<form>`，输入框内按 Enter 不提交**；🔴 进入页面**不自动加载**                                               | —                              | 🟡 两者都已改   |
| `[x]` | 请求 `GET /api/v1/devices/{id}/history`，`buildQuery` 丢弃空值，`credentials:include` + `no-store`                   | `services/fleetApi.ts:102-109` | 🟢              |
| `[x]` | 返回 newest-first，前端 **reverse 成 oldest-first** 再交给回放                                                       | `:148`                         | 🟡 改为显式排序 |
| `[x]` | ⚠️ 服务端实际最多返回 `MAX_HISTORY_POINTS`（默认 500），而 schema 允许 5000、前端默认请求 1000 —— **三处口径不一致** | `persistence.ts:421`           | ⚠️ 前端那处已消 |
| `[x]` | 未选设备 → warning toast「请先选择设备」，**不发请求、不停回放**                                                     | `:132-135`                     | 🔴 状态不存在   |
| `[x]` | 成功但 0 条 → info toast「该设备在所选时间范围内没有历史轨迹数据」                                                   | `:151`                         | 🟡 改为空态     |
| `[x]` | 失败 → 清空 + error toast「加载历史数据失败，请稍后重试」                                                            | `:156-160`                     | 🟡 改为失败态   |
| `[x]` | 按钮 disabled + 文案「加载中…」；地图区「正在加载历史轨迹…」                                                         | `:191-193,233-235`             | 🟢              |

**核销说明：**

- **设备下拉、以及"未选设备"这个状态，一起废弃。** 设备来自 `:deviceId`，所以"没选设备"在新实现里
  不是一种可达状态，而不是一种被省略的提示。
- **最大点数输入框删掉，并且不发 `limit`。** 它的 `min/max` 因为没有 `<form>` 从不生效，而服务端
  无论如何按自己的 `MAX_HISTORY_POINTS` 硬夹 —— 所以这个控件让你填 5000、安静地给你 500。
  不发 `limit` 意味着由部署的上限说话；代价是前端不知道那个上限是多少，于是改为**如实报出实际
  覆盖的时间跨度**（「已载入 N 条采样，覆盖 X – Y」）。窗口被截断时人能直接看见，而不必接受一个
  前端做不出的诊断 —— "上限截断了"和"这台车当时停着没上报"从这里看是一模一样的。
  ⚠️ 9.14 只消掉了前端那一处：**schema 允许 5000 而服务端硬夹 500 仍然不一致**，归后端批次。
- **「清除时间」按钮废弃。** 空窗口的语义是"给我最新的一段"，而那正是快捷范围在做的事，只是说得
  更准确。留一个语义与三个按钮重叠的按钮，只是多一种到达同一处的方式。
- **reverse 改成显式排序。** 依赖 newest-first 是对后端顺序下的一个赌注；Mongo 路径确实是
  `sort({ts:-1})`，但内存回退是另一条代码路径。排序是断言，reverse 是赌注。
- **窗口的结束是那一分钟的结束，不是它的开始。** `datetime-local` 的值是一个*分钟*，而
  `slice(0, 16)` 会把 `now` 抹掉最多 59 秒 —— 于是「最近 1 小时」请求的窗口在一分钟前就结束了，
  最新的采样被安静地丢掉。这条是浏览器套件抓到的（滑块 `max="1"`，6 条只回来 2 条），单测看不见，
  因为没人会想到去断言"结束时间不早于现在"。v1.0.0 有同一处取整。
- **toast 改成空态与失败态。** toast 会滚走，而这两件事都要在人**正在看的地方**说 —— 那是地图区。

### 5.2 回放条

|       | 项                                                                                   | 现状                          | 去留                                 |
| ----- | ------------------------------------------------------------------------------------ | ----------------------------- | ------------------------------------ |
| `[x]` | 播放/暂停按钮，文案两态，`:disabled="!samples.length"`                               | `:257-259`                    | 🟢                                   |
| `[x]` | 重播按钮                                                                             | `:260-262`                    | 🟢                                   |
| `[x]` | 进度滑块，受控写法（`:value` + `@input`），`aria-label="回放进度"`                   | `:269-278`                    | 🟢 **axe critical 修复，名称已保留** |
| `[x]` | 倍速下拉 4 档，`aria-label="回放速度"`                                               | `:279-289`                    | 🟢 同上                              |
| `[x]` | 间隔 `max(80, 600/speed)`：0.5×=1200ms / 1×=600ms / 2×=300ms / 4×=150ms              | `useHistoryPlayback.ts:41-43` | 🟢                                   |
| `[x]` | 进度文本 `progressLabel` 是 **1-based**（`1 / N`）而 cursor 是 0-based               | `:54-59`                      | 🟢                                   |
| `[x]` | 到末尾停在最后一帧**不回绕**；再点播放则从 0 重播                                    | `:69-90`                      | 🟡 改为到达即停                      |
| `[x]` | 播放中拖动进度条**不暂停**，定时器从新位置继续                                       | `:269-278`                    | 🟡 已改为拖动即暂停                  |
| `[x]` | 改倍速仅在播放中重定时                                                               | `:102-107`                    | 🟢                                   |
| `[x]` | 卸载时 `stopPlayback`；composable 非全局单例，重进页面是全新空状态                   | `:109`                        | 🟢                                   |
| `[x]` | ⚠️ **切换设备下拉不重置回放** —— 旧样本继续播，而设备名/trail key/场景立刻切到新设备 | 无 `watch(deviceId)`          | ⚠️ 已修                              |

**核销说明：到达即停，而不是到达后的下一个 tick 才停。** v1.0.0 在最后一帧之后还要等一个周期才
清掉定时器，所以按钮在轨迹已经结束后仍显示"暂停"最多 600ms。**换设备时先清空再请求**，所以上一台
车的轨迹不会挂在这一台的名字下面。

### 5.3 采样详情面板（8 个字段）

|       | 标签                                                              | 取值                           | 格式化                                                                                        |
| ----- | ----------------------------------------------------------------- | ------------------------------ | --------------------------------------------------------------------------------------------- |
| `[x]` | 采样时间                                                          | `currentSample.ts`             | `formatDateTime`；⚠️ `ts` 缺失时 `toTimestampMs` 回退 `Date.now()`，**显示当前时间而非 `--`** |
| `[x]` | 速度                                                              | `vehicleInfo.speed`            | `formatNumber(v,2," m/s")`                                                                    |
| `[x]` | 电量                                                              | `vehicleInfo.soc`              | `formatNumber(v,1,"%")`                                                                       |
| `[x]` | 任务状态                                                          | `taskStatus`                   | `formatEnum(taskStatusMap)`                                                                   |
| `[x]` | 融合 X / Y                                                        | `fusionLoc.x/y`                | `formatNumber(v,2)` 无单位                                                                    |
| `[x]` | 航向 yaw                                                          | `fusionLoc.yaw`                | `formatNumber(v,3)` 弧度无单位                                                                |
| `[x]` | 场景                                                              | `sceneName \|\| activeSceneId` | 字面 `--`                                                                                     |
| `[x]` | 空态「暂无采样 / 加载轨迹后，这里会显示当前回放位置的遥测详情。」 |                                | 🔴 不再可达：整个面板在无采样时不渲染                                                         |

**核销说明：无 `ts` 的采样显示 `--`，不是当前时间。** `formatDateTime` 内部回退到 `Date.now()`，
所以照抄会把一条没有时间戳的采样标成"这一秒"。

**已映射但从不渲染**（死映射）：`deviceName`、`online`、三个 code、`gps`、`vehicleInfo.controlMode/gear/omega`、
`speedLimit`、`stamp`。**连映射都没有**：`platformTaskStatus`、`extra`、`defaultSceneId`、`meta.*`。
→ ✅ 13C 的设备详情把这些接上了（实时 tab 的六个面板 + 报码解读）。回放帧刻意**不**并进实时快照：
一个采样没带的字段必须显示为空，否则会把这一秒的报码印在一小时前的那一帧上。唯一的例外是身份
（车名、topic）—— 那是车的属性，不是那一刻的属性。

### 5.4 历史页地图与轨迹

|       | 项                                                                                                             | 现状       | 去留           |
| ----- | -------------------------------------------------------------------------------------------------------------- | ---------- | -------------- |
| `[x]` | `:scene-devices="[]"` —— 无僚车、无僚车轨迹、`getDeviceTone` 实际永不被调用                                    | `:227`     | 🟢             |
| `[x]` | ⚠️ `trailsForMap` 每次 cursor 变化**从 index 0 重走整段前缀**，且每 index 调 `measurementsOf` 两次，全程 O(N²) | `:102-114` | ⚠️ 已修        |
| `[x]` | 回放轨迹**没有** `TRAIL_MIN_DISTANCE` 去抖、**没有** `TRAIL_MAX_POINTS` 截断（与实时不同）                     | 同上       | 🟢 确认保留    |
| `[x]` | 5 个互斥空态：地图 / 加载中 / 没有历史数据（含 MongoDB 说明）/ 缺 ROS 位姿 / 初始引导                          | `:223-253` | 🟡 第 5 支重做 |
| `[x]` | ⚠️ 第 5 个分支同时承担"初始未加载"与"有数据但场景定义缺失"两种情形                                             | `:250-253` | ⚠️ 已拆        |

**核销说明：**

- **O(N²) 的修法是两处**：位姿改为每条轨道只提取一次（5000 样本 4x 播放：约 1250 万次 → 5000 次），
  轨迹增量维护（cursor +1 就 push 一个点，只有拖动才重建）。测试断言的是 **`poseOf` 被调用几次**，
  不是耗时 —— 在共享 CI runner 上断言耗时是在断言 runner。
  **没有**顺手声称把 SVG path 的重建也修了：把不断变长的折线序列化成 `d` 是每帧 O(n)，而且那是
  画折线本身固有的；在这个端点实际返回的几百个点上，那是字符串拼接而不是解析。
- **去抖与截断确认不加。** 两者对实时的滚动轨迹是对的（抖动噪声、无界增长），对一个有界的历史窗口
  是错的：去抖会丢掉人正在拖过去的点，截断会藏掉窗口的开头。
- **第 5 个空态拆成两个**：「缺少场景地图定义」有自己的分支并**说出是哪个场景**。原来那一支会让人
  去按一个已经按过的按钮。

## 6. 告警中心（`AlertsView.vue` 251 + `useAlertAck.ts` 65）

|       | 项                                                                                                                       | 现状                                | 去留                  |
| ----- | ------------------------------------------------------------------------------------------------------------------------ | ----------------------------------- | --------------------- |
| `[ ]` | 4 个严重度桶（全部 / 严重 / 预警 / 提示），`aria-pressed` 与 `active` 类同源                                             | `:144-166`                          | 🟢 **axe 修复**       |
| `[ ]` | ⚠️ 桶计数与 header 计数**不受任何筛选影响且含已确认项**，与默认列表条数不一致                                            | `:45-50`                            | ⚠️ 见 9.17            |
| `[ ]` | 单选、不支持多选、不支持再点取消回到 all                                                                                 | `:106-108`                          | 🟡                    |
| `[ ]` | 设备下拉：**只列出当前有告警的设备**；选中设备告警清零后选项消失但筛选值保留 → 列表空                                    | `:56-64`                            | 🟡                    |
| `[ ]` | 搜索匹配 **title / deviceName / info / detail** 四项，substring 不区分大小写，**无防抖**                                 | `:67,79-84`                         | 🟡 加防抖             |
| `[ ]` | ⚠️ `deviceName` 无空值兜底，缺失时把字面 `"undefined"` 拼进匹配串                                                        | `:81`                               | ⚠️ 见 9.18            |
| `[ ]` | 「显示已确认（N）」复选框，**默认隐藏已确认**                                                                            | `:28,75-77,184`                     | 🟢                    |
| `[ ]` | 四条筛选按 severity → device → ack → keyword 顺序 AND 组合                                                               | `:68-86`                            | 🟢                    |
| `[ ]` | 分页 `PAGE_SIZE = 12`（无 UI 可调）；单页时整条隐藏；筛选变更回第 1 页；总页数缩小时钳位                                 | `:23,89-104,241-248`                | 🟡                    |
| `[ ]` | 🔴 无跳页输入、无页码列表、无每页条数选择、**无 URL 同步**（刷新回默认）                                                 | —                                   | 🟡 URL 同步值得做     |
| `[ ]` | 排序：严重度（critical<warning<notice）为主键、`ts` 降序为次键，稳定排序；**无用户可选排序**                             | `:33-43`                            | 🟡                    |
| `[ ]` | ⚠️ `ts` 缺失的告警因 `Date.now()` 回退被排到最前                                                                         | `fleetNormalize.ts:61-74`           | ⚠️ 9.19               |
| `[ ]` | 行内 8 项：标题 / 严重度徽标 / 说明（三级回退）/ 设备名 / 来源（4 条映射表）/ 报码（`code` 为真才显示）/ 时间 / 确认按钮 | `:217-232`                          | 🟢                    |
| `[ ]` | 视觉编码：`data-severity` 边框、`focused`（= 当前选中设备）brand 边框、`acknowledged` `opacity:.55`                      | `:210-213`                          | 🟢                    |
| `[ ]` | 🔴 行本身无 `@click`、不可聚焦（点击行不选中设备）                                                                       | —                                   | 🟡 应该可点           |
| `[ ]` | 确认按钮文案两态；⚠️ **无 `aria-pressed`、无 `aria-label`**                                                              | `:230-232`                          | ⚠️ 9.20               |
| `[ ]` | 「确认当前筛选」作用于 `filteredAlerts` **全集**（不只当前页），`:disabled` 于空集                                       | `:118-120,187-194`                  | 🟢                    |
| `[ ]` | 「清除已确认」清**全局**（含筛选外）                                                                                     | `:195-202`                          | 🟡 文案应更明确       |
| `[ ]` | 🔴 两个批量操作**无 toast、无确认弹窗**；默认隐藏已确认时点批量确认会让列表瞬间清空                                      | —                                   | 🟡 补反馈             |
| `[ ]` | 确认状态存 `localStorage["navfleet:acked-alerts"]`，`JSON.stringify([...Set])`                                           | `useAlertAck.ts:15,32`              | 🔴 Phase 16A 改为落库 |
| `[ ]` | alert id 天然含 deviceId，所以确认只影响该设备该告警                                                                     | `fleetNormalize.ts:208,224,234,392` | 🟢                    |
| `[ ]` | 🔴 无时间戳、无过期、与登录用户无关（同机换账号仍见同一份）                                                              | —                                   | 🔴 Phase 16A 修       |
| `[ ]` | 2 个互斥空态，判据是 `stats.total`：「当前筛选条件下没有告警。」/「当前没有活动告警，系统运行正常。」                    | `:236-238`                          | 🟢                    |
| `[ ]` | 🔴 空态无 `role="status"` / `aria-live`                                                                                  | —                                   | 🟡                    |
| `[ ]` | 🔴 **完全不调用 `/api/v1/alerts`**，告警全部来自实时快照派生                                                             | 见第 8 节                           | 🔴 Phase 16B 接上     |

## 7. 设置 / 登录 / 404

### 7.1 设置页（`SettingsView.vue` 154 行）

定位刻意收窄（文件头注释）：只读监控平台，此页只放客户端偏好与本地状态。

|       | 项                                                                                                        | 现状                                                           | 去留                      |
| ----- | --------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------- | ------------------------- |
| `[ ]` | 外观组：3 个 radio（深色/浅色/跟随系统），`name` 共享，`:checked` 受控于 store，选中即落盘                | `:88-103`                                                      | 🟢                        |
| `[ ]` | 「当前生效：深色/浅色」显示**解析后**的实际主题                                                           | `:35,90`                                                       | 🟢                        |
| `[ ]` | 做成 radio group 的理由：header 的循环按钮在看不见当前图标时不可用                                        | 文件头注释                                                     | 🟢 保留两处入口           |
| `[ ]` | 本地数据组：已确认告警（N 条）+ 场景视图记忆（N 个），各一个清除按钮，`:disabled` 于零                    | `:105-140`                                                     | 🟡 Phase 16A 后前者语义变 |
| `[ ]` | 「已确认告警」清除是 `setItem(key,"[]")` **不是 removeItem**；「场景视图」是 `removeItem`                 | `useAlertAck.ts:59-62`；`useSceneViewportPersistence.ts:72-83` | 🟢                        |
| `[ ]` | `savedViewCount` 在 **setup 期间**读取（不是 `onMounted`），避免首帧闪 0                                  | `:39-43`                                                       | 🟢                        |
| `[ ]` | 两个清除各有 success toast，带数量                                                                        | `:51,58`                                                       | 🟢                        |
| `[ ]` | 连接诊断 `<dl>` **7 行**：车队名称 / MQTT 主题模板 / 后端接口 / 实时推送 / 数据来源 / 最近更新 / 设备总数 | `:61-77`                                                       | 🟢                        |
| `[ ]` | 后端接口与实时推送带 `data-tone`（ok/warning/critical）上色                                               | `settings.css:145-155`                                         | 🟢                        |
| `[ ]` | 设置页**不触碰** `navfleet:theme` / `map-mode` / `fleet-collapsed`；全站无"清空全部存储"                  | —                                                              | 🟡 建议加一个             |
| `[ ]` | `.settings-radio:focus-within` 焦点态（全站少数有 focus 样式的地方之一）                                  | `settings.css:91`                                              | 🟢                        |

### 7.2 登录（`LoginForm.vue` 92 行）

|       | 项                                                                                                                  | 现状               | 去留             |
| ----- | ------------------------------------------------------------------------------------------------------------------- | ------------------ | ---------------- |
| `[ ]` | `aria-labelledby="login-heading"` + `:aria-busy="pending"`                                                          | `:36-41`           | 🟢               |
| `[ ]` | 两个字段带 `label[for]` / `autocomplete` / `required` / `:disabled="pending"`                                       | `:48-79`           | 🟢               |
| `[ ]` | 有错时 `aria-invalid="true"` + `aria-describedby="login-error"` 条件绑定                                            | `:48-79`           | 🟢               |
| `[ ]` | 错误 `<p role="alert">`（assertive，不是 polite）                                                                   | `:85`              | 🟢               |
| `[ ]` | `onMounted` 自动聚焦用户名                                                                                          | `:17-19`           | 🟢               |
| `[ ]` | 提交按钮 `:disabled="pending"`，文案两态                                                                            | `:87-89`           | 🟢               |
| `[ ]` | ⚠️ 空字段**静默不提交、无提示**，可见反馈只来自原生 `required`                                                      | `:21-26`           | ⚠️ 9.21          |
| `[ ]` | 失败文案三档：401「用户名或密码错误」/ 其他非 2xx「登录失败，请稍后重试」/ fetch 抛错「无法连接服务器，请检查网络」 | `useAuth.ts:94-96` | 🟢               |
| `[ ]` | 成功后**无路由跳转**，靠 `status` 变化换出整个外壳                                                                  | `App.vue:71-76`    | 🟢               |
| `[ ]` | 🔴 无"记住我"、无找回密码、无验证码                                                                                 | —                  | 🟡 Phase 15 评估 |

### 7.3 404（`NotFoundView.vue` 21 行）

|       | 项                                                               | 现状                          | 去留 |
| ----- | ---------------------------------------------------------------- | ----------------------------- | ---- |
| `[ ]` | 通配路由 `/:pathMatch(.*)*` 渲染真 404（此前是静默重定向到 `/`） | `router/index.ts:42-49`       | 🟢   |
| `[ ]` | e2e 断言：404 页面渲染时外壳/导航/会话完好                       | `e2e/specs/not-found.spec.ts` | 🟢   |

### 7.4 错误边界与骨架

|       | 项                                                                                                           | 现状                      | 去留                |
| ----- | ------------------------------------------------------------------------------------------------------------ | ------------------------- | ------------------- |
| `[ ]` | `onErrorCaptured` 捕获子树 render/生命周期/watcher 抛错，先 `console.error` 保栈，再 `return false` 阻止冒泡 | `ErrorBoundary.vue:19-29` | 🟢                  |
| `[ ]` | 降级 UI：`role="alert"` + 徽标 + 标题「页面渲染失败」+ 提示 + `summary · info` + 重试按钮                    | `:41-53`                  | 🟢                  |
| `[ ]` | `resetKey` = `route.fullPath` → **路由变化自动清除降级态**                                                   | `:37`；`App.vue:175`      | 🟢                  |
| `[ ]` | 边界在外壳**内部**，视图崩溃时 header/nav 存活                                                               | `App.vue:166-167`         | 🟢                  |
| `[ ]` | `SkeletonBlock` props：`rows`（默认 1）、`variant`（`line` 14px / `value` 27px / `card` 74px）               | `SkeletonBlock.vue:10-18` | 🟢                  |
| `[ ]` | `aria-hidden` 挂在**外层 stack** 上，语义由容器的 `aria-busy` 承担                                           | `:22`，注释 `:1-9`        | 🟢 **理由要保留**   |
| `[ ]` | `SkeletonBlock` 目前只被 Dashboard 使用                                                                      | —                         | 🟡 新前端应全站铺开 |

## 8. 全局机制

### 8.1 路由

|       | 项                                                                                                                                                                                             | 现状                    | 去留                           |
| ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------- | ------------------------------ |
| `[ ]` | 5 条路由全部懒加载                                                                                                                                                                             | `router/index.ts:17-50` | 🟢                             |
| `[ ]` | `createWebHashHistory()`；理由：nginx **未配 history fallback**、Dockerfile 无自定义 conf                                                                                                      | `:1-8,53`               | 🟡 迁 web history 要同时改部署 |
| `[ ]` | ⚠️ 5 个 `meta.title` 全部定义、**零消费**，页面标题恒为 index.html 的值                                                                                                                        | `:22-48`                | ⚠️ 9.22                        |
| `[ ]` | 守卫：`dashboard` 无条件放行（重定向不可能成环、首次导航不阻塞）→ `unknown` 则等待 → `anonymous` 重定向 `{replace:true}` → 超时（8s）打 warn 后**放行**（此时 App 渲染加载屏，视图根本不挂载） | `guards.ts:89-111`      | 🟢                             |
| `[ ]` | `waitForAuthResolution` 用 `flush:"sync"` watch + timeout，`finish()` 保证先清 timer 再 stop watcher                                                                                           | `:46-77`                | 🟢                             |
| `[ ]` | 守卫接口刻意收窄为 `{status}` 与 `Pick<…,"name"\|"fullPath">`，便于注入假对象                                                                                                                  | `:16-21`                | 🟢                             |
| `[ ]` | 🔴 **没有一条路由带 `meta.roles`**                                                                                                                                                             | —                       | 🔴 Phase 15C 补                |

### 8.2 会话（`useAuth.ts`）

|       | 项                                                                                               | 现状                         | 去留          |
| ----- | ------------------------------------------------------------------------------------------------ | ---------------------------- | ------------- |
| `[ ]` | 模块级单例 `reactive`，`status: unknown \| authenticated \| anonymous`                           | `:30-35`                     | 🟡 迁进 store |
| `[ ]` | 统一 `request()` 带 `credentials:"include"`；⚠️ `...options` 在后，传 `headers` 会整体覆盖默认头 | `:21-26`                     | 🟡            |
| `[ ]` | 刷新周期 10 分钟                                                                                 | `:19`                        | 🟢            |
| `[ ]` | ⚠️ 刷新是 **fire-and-forget**，不看响应；401 不改状态、不登出、不提示                            | `:46-51`                     | ⚠️ 9.23       |
| `[ ]` | `/api/auth/me` **只在 `App.vue` 的 `onMounted` 调一次**，守卫从不主动调                          | `:66-79`；`App.vue:85`       | 🟢            |
| `[ ]` | 登出无论成败都 `setAnonymous()`                                                                  | `:103-110`                   | 🟢            |
| `[ ]` | ⚠️ **无全局 401 拦截**：`fleetApi` 把任何非 2xx 统一抛 `HTTP n`，不区分 401，也不跳登录          | `services/fleetApi.ts:70-72` | ⚠️ 9.24       |

### 8.3 主题（`useTheme.ts`）

|       | 项                                                                                                                                                                   | 现状                       | 去留                                |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------- | ----------------------------------- |
| `[ ]` | 3 个选项，key `navfleet:theme`，非法值回落 `system`                                                                                                                  | `:6,18-28`                 | 🟢                                  |
| `[ ]` | 应用到 `<html data-theme>` + `style.colorScheme` 两处                                                                                                                | `:44-50`                   | 🟢                                  |
| `[ ]` | `:root` 是**深色基线**，`[data-theme="light"]` 覆盖；`index.html` 不预置 → App setup 时才写入                                                                        | `tokens.css:1,64`          | 🟡 新前端建议内联脚本预置，避免闪白 |
| `[ ]` | `matchMedia` 只在 `system` 模式跟随                                                                                                                                  | `:74-80`                   | 🟢                                  |
| `[ ]` | ⚠️ media 监听**注册后永不移除**，也未回退 `addListener`                                                                                                              | `:76-80`                   | ⚠️ 9.25                             |
| `[ ]` | `cycleTheme` 顺序 `dark → light → system → dark`                                                                                                                     | `:62-67`                   | 🟢                                  |
| `[ ]` | 导出 `readonly(state)`                                                                                                                                               | `:82`                      | 🟢                                  |
| `[ ]` | **37 个 token**（8 几何 + 29 语义）双主题成对；`--brand-contrast`（实心 brand 面）与 `--brand-ink`（rgba 薄底）的区分**必须保留** —— 明度需求相反，这是 axe 才发现的 | `tokens.css`               | 🟢 **Phase 11D 的输入**             |
| `[ ]` | 6 个 ROS 专用 token 双主题都要给                                                                                                                                     | `tokens.css:56-61,100-105` | 🟢                                  |

### 8.4 通知（`useNotifications.ts` + `NotificationHost.vue`）

|       | 项                                                                                                                  | 现状                         | 去留        |
| ----- | ------------------------------------------------------------------------------------------------------------------- | ---------------------------- | ----------- |
| `[ ]` | `notify(message, {type, timeout, dedupeKey}): number`，默认 `info`                                                  | `:50-51`                     | 🟢          |
| `[ ]` | 去重：同 key 存活期内重复调用**直接返回 -1，不入列、不重置计时**                                                    | `:52-57`                     | 🟢          |
| `[ ]` | 默认时长：info 4s / success 3s / warning 6s / error 8s；`timeout: 0` 常驻                                           | `:27-32,62-68`               | 🟢          |
| `[ ]` | 容器 `aria-live="polite"` + 每条 `role="status"`，右上固定，`max-width: min(380px, 100vw-36px)`                     | `NotificationHost.vue:15-17` | 🟢          |
| `[ ]` | 每条带图标 + 关闭按钮（`aria-label="关闭"`）                                                                        | `:17-22`                     | 🟢          |
| `[ ]` | **12 个 toast 触发点**（ws 恢复/中断、bootstrap 失败、两类全局错误、历史 3 条、overlay/metadata 2 条、设置页 2 条） | 见各处                       | 🟢 逐条核销 |

### 8.5 全局错误（`globalErrorHandlers.ts`）

|       | 项                                                                                                     | 现状           | 去留 |
| ----- | ------------------------------------------------------------------------------------------------------ | -------------- | ---- |
| `[ ]` | 在 `mount()` **之前**安装，所以 bootstrap 期间的失败也能上报                                           | `main.ts:8-11` | 🟢   |
| `[ ]` | 幂等标志                                                                                               | `:33-36`       | 🟢   |
| `[ ]` | `window.onerror`：notify 后**链式调用**先前 handler（不是替换），`return false` 让浏览器继续打印原始栈 | `:38-51`       | 🟢   |
| `[ ]` | `unhandledrejection`：notify，**不调 `preventDefault()`**，控制台仍报告                                | `:53-60`       | 🟢   |
| `[ ]` | `summarize` 四级降级；dedupeKey 内嵌 summary，同错误循环只收敛一条                                     | `:15-26,43-44` | 🟢   |

### 8.6 store 对外契约 —— 28 个键（不是 30）

`defineStore("fleet", setup)`，返回对象 28 键（`stores/fleet.ts:731-760`）。**Phase 12/13 搬迁时逐个核销。**

| #   | 键                        | 类型             | 职责                                                                                                                                                                                                                             |
| --- | ------------------------- | ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `state`                   | state            | reactive 根：fleetName / topicPattern / devicesById / formationsById / selectedDeviceId / selectedFormationId / selectedMapMode / lastSource / lastUpdateAt / sceneDefinitions / pendingSceneLoads / trailsByDeviceId / realtime |
| 2   | `formations`              | computed         | 补齐 deviceCount / onlineCount / 推断 sceneId                                                                                                                                                                                    |
| 3   | `sortedDevices`           | computed         | deviceId 稳定升序                                                                                                                                                                                                                |
| 4   | `filteredDevices`         | computed         | 按选中编队过滤                                                                                                                                                                                                                   |
| 5   | `sortedFormations`        | computed         | formationId 升序                                                                                                                                                                                                                 |
| 6   | `selectedDevice`          | computed         | —                                                                                                                                                                                                                                |
| 7   | `selectedFormation`       | computed         | —                                                                                                                                                                                                                                |
| 8   | `summary`                 | computed         | `{totalCount, onlineCount, alertTotal, focusName}`                                                                                                                                                                               |
| 9   | `groupedAlerts`           | computed         | 三桶，每条附 deviceId/deviceName，桶内 ts 倒序                                                                                                                                                                                   |
| 10  | `bootstrapPending`        | computed         | 驱动骨架屏                                                                                                                                                                                                                       |
| 11  | `sceneDevices`            | computed         | 场景图要一起画的车（排除 `rosMapEnabled === false`）                                                                                                                                                                             |
| 12  | `formationSceneId`        | computed         | 编队 sceneId → 设备 sceneId → `""`                                                                                                                                                                                               |
| 13  | `hasPose`                 | 纯函数（再导出） | —                                                                                                                                                                                                                                |
| 14  | `round`                   | 纯函数（再导出） | —                                                                                                                                                                                                                                |
| 15  | `formatDateTime`          | 纯函数（再导出） | —                                                                                                                                                                                                                                |
| 16  | `getSceneDefinition`      | getter           | `sceneDefinitions[id] \|\| sceneCatalog[id] \|\| null`                                                                                                                                                                           |
| 17  | `getDeviceTone`           | 纯函数           | 五级优先级                                                                                                                                                                                                                       |
| 18  | `buildFleetSnapshot`      | action(读)       | 深拷贝快照，供 window API                                                                                                                                                                                                        |
| 19  | `bootstrap`               | action(async)    | 后端引导，失败退空态                                                                                                                                                                                                             |
| 20  | `registerWindowApi`       | action           | 挂 `window.vehicleDashboard` 6 个方法                                                                                                                                                                                            |
| 21  | `selectDevice`            | action           | 带 `preserveFormation` 选项                                                                                                                                                                                                      |
| 22  | `selectFormation`         | action           | ⚠️ 隐式强制 scene 模式且不落盘                                                                                                                                                                                                   |
| 23  | `clearFormationSelection` | action           | —                                                                                                                                                                                                                                |
| 24  | `setMapMode`              | action           | 写 localStorage                                                                                                                                                                                                                  |
| 25  | `trailsByDeviceId`        | computed         | —                                                                                                                                                                                                                                |
| 26  | `clearTrail`              | action           | 整体替换对象以触发响应                                                                                                                                                                                                           |
| 27  | `retryBootstrap`          | action(async)    | **返回 `ready` 布尔**供横幅按钮判断                                                                                                                                                                                              |
| 28  | `disconnectRealtime`      | action           | 手动关闭标志 + 清全部定时器                                                                                                                                                                                                      |

🟡 **改造建议**：28 键 + 761 行 + 六种职责（state / 归一化入口 / 9 个 computed / 整个 WS 传输层 /
场景加载 / window 调试 API）应在 Phase 12 拆开。WS 传输层尤其应该独立。

### 8.7 实时链路

|       | 项                                                                                                                                                                                            | 现状                       | 去留                         |
| ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------- | ---------------------------- |
| `[ ]` | WS URL：`(https?→wss/ws)//window.location.host/ws`，**不读环境变量、不考虑子路径**                                                                                                            | `:491-494`                 | 🟡 迁 web history 时要一起看 |
| `[ ]` | 心跳 20s / pong 超时 10s / 退避 base 1s / 上限 30s                                                                                                                                            | `:498-501`                 | 🟢                           |
| `[ ]` | 心跳：`readyState !== OPEN` 跳过；send 抛错 return；发成功后设 10s 超时，到期 `ws.close()` 强制走重连 —— 为的是识别"永不触发 close 的黑洞连接"                                                | `:533-555`                 | 🟢 **理由要保留**            |
| `[ ]` | 退避 `min(1000 × 2^attempts, 30000)`，**先自增再排定**，有 timer 去重                                                                                                                         | `:520-531`                 | 🟢                           |
| `[ ]` | 连接单例保护（`ws` 非空直接 return）；构造抛错也走重连                                                                                                                                        | `:557-618`                 | 🟢                           |
| `[ ]` | `open`：仅当 `reconnectAttempts > 0` 才提示"已恢复"（首连不提示）                                                                                                                             | `:571-579`                 | 🟢                           |
| `[ ]` | `close`：仅当 `wasReady && !手动关闭` 才提示"中断，正在自动重连…"                                                                                                                             | `:604-613`                 | 🟢                           |
| `[ ]` | `error` 回调**故意空**：error 后必跟 close，统一由 close 驱动重连                                                                                                                             | `:615-617`                 | 🟢                           |
| `[ ]` | 消息 3 类：`pong`（清超时）/ `fleet.snapshot`（source `ws`）/ `fleet.delta`（source `mqtt`），其他静默丢弃；整体 try/catch                                                                    | `:582-602`                 | 🟢                           |
| `[ ]` | 快照 vs delta 的真实差异不在 type 分支，而在 `normalizePayload` 按**载荷形状**给出的 `replace` 标志：数组或含 `devices` → 整表替换；含 `topic`+`payload` 或裸对象 → 逐设备 `mergeDevice` 合并 | `:159-216,400-431`         | 🟢                           |
| `[ ]` | 轨迹记录：`TRAIL_MIN_DISTANCE` 去抖 + `TRAIL_MAX_POINTS` 尾部裁剪；`replace` 时删除已消失设备的轨迹                                                                                           | `:367-398`                 | 🟢                           |
| `[ ]` | REST 引导顺序：场景目录（失败不阻断）→ snapshot → `apiReady=true` → ingest → 连 WS                                                                                                            | `:659-675`                 | 🟢                           |
| `[ ]` | 引导失败 → `apiReady=false` + error toast → `bootstrapEmptyState()` 灌本地兜底                                                                                                                | `:667-680`                 | 🟢                           |
| `[ ]` | ⚠️ `data-defaults.ts` 的 `sceneCatalog = {}` 与 `fallbackFleetPayload.devices = []` 都是**永久空**，三处查表恒不命中                                                                          | `data-defaults.ts:7,16-21` | 🔴 要么填要么删              |

### 8.8 全部浏览器存储 key（5 个）

|       | key                           | 存储        | 值形态                                               | 写入方                        |
| ----- | ----------------------------- | ----------- | ---------------------------------------------------- | ----------------------------- |
| `[ ]` | `navfleet:theme`              | local       | `light\|dark\|system`                                | `useTheme.setPreference`      |
| `[ ]` | `navfleet:map-mode`           | local       | `gps\|scene`                                         | `store.setMapMode`            |
| `[ ]` | `navfleet:acked-alerts`       | local       | `JSON` 字符串数组                                    | `useAlertAck.persist`         |
| `[ ]` | `navfleet:fleet-collapsed`    | local       | `"1"\|"0"`                                           | `DashboardView.toggleFleet`   |
| `[ ]` | `navfleet:ros-scene-views:v2` | **session** | `Record<sceneId, {centerX,centerY,scale,updatedAt}>` | `useSceneViewportPersistence` |

两处生命周期选择都有明确理由，**不要统一成一种**：视图记忆用 session（陈旧视图比重新取景更困扰），
地图模式用 local（F5 后被踢回 GPS 是实际投诉）。5 个 key 全部包 try/catch，隐私模式下静默降级为内存态。

## 9. 盘点中发现的缺陷 —— 新前端要修掉，照抄就是失职

这些不是"等价"目标，是**反向**目标。按影响排序。

| #    | 缺陷                                                                                                                                                                                                                                                                       | 位置                                                         | 影响                                               |
| ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------ | -------------------------------------------------- |
| 9.1  | **`formatNumber(null)` 渲染成 `0.00` 而不是 `--`**（`Number(null) === 0`）。而 `createDefaultDevice` 把 `soc/speed/omega/x/y/yaw/limit` 全初始化为 `null` → **没有遥测的设备显示 0，不是占位符**。位姿卡尤其自相矛盾：徽标写"无数据"，同一张卡的数值行写 `x：0.00 y：0.00` | `utils/formatters.ts:12-18`；`lib/fleetNormalize.ts:132-141` | **高**：操作员会把"没数据"读成"速度为 0、电量为 0" |
| 9.2  | **`trailsForMap` 是 O(N²)**：每次 cursor 变化从 index 0 重走整段前缀，每 index 调 `measurementsOf` 两次，并生成新对象字面量迫使下游 SVG path 全量重建。limit 上限 5000，4× 播放 150ms/帧                                                                                   | `HistoryView.vue:102-114`                                    | **高**：唯一确定的前端性能缺陷                     |
| 9.3  | **wheel 每个刻度一次同步 sessionStorage 读写 + 两次 `getBoundingClientRect`**（两次强制布局）；`pointermove` 直接写 viewport 无 rAF 合帧；ResizeObserver 回调同样每帧一次存储写                                                                                            | `useSvgViewport.ts:245,470-492,519-543`                      | 中                                                 |
| 9.4  | **栅格底图 HTTP 失败完全静默** —— 破图，无错误 UI、无 toast，与点云/overlay 的处理不一致                                                                                                                                                                                   | `RosSceneMap.vue:390-400`                                    | 中                                                 |
| 9.5  | **overlay 与 metadata 无 request-id 竞态守卫**（点云有），慢请求可能在场景切换后落地                                                                                                                                                                                       | `useSceneOverlay.ts:40-80`                                   | 中                                                 |
| 9.6  | **`selectFormation` 强制切 scene 模式但不落盘**，与 `setMapMode` 口径不一致 → 刷新后模式跳回旧值                                                                                                                                                                           | `stores/fleet.ts:467-474`                                    | 中                                                 |
| 9.7  | **两个 `.ros-empty` 覆盖层 `inset:0` 且无 `pointer-events:none`**（`.gps-map-overlay` 有），显示时吞掉滚轮与指针事件                                                                                                                                                       | `map-panel.css:63-73`                                        | 中                                                 |
| 9.8  | **AMap 已存在脚本分支可能永不 settle**：若该脚本已加载完却没暴露 `window.AMap`，`load`/`error` 都不会再触发                                                                                                                                                                | `utils/amap.ts:50-63`                                        | 中                                                 |
| 9.9  | **全站几乎没有自定义 focus 样式** —— 只有 skip-link / 登录输入框 / 设置页 radio 有；仪表盘所有控件靠浏览器默认焦点环，而它们又被设了 `border:0`                                                                                                                            | `grep focus` 于 CSS 目录                                     | 中（a11y，axe 抓不到）                             |
| 9.10 | **GPS 地图的 `aria-label` 挂在无 role 的 div 上**，不可靠暴露；ROS 图的 `role="img"` 也让内部标记对 AT 完全不可见                                                                                                                                                          | `GpsMap.vue:290`；`RosSceneMap.vue:365`                      | 中                                                 |
| 9.11 | **离线横幅在冷启动期间必然出现**（`apiReady` 初值 false，且 WS 状态不影响它）→ 每次进入应用都闪一下"后端不可用"                                                                                                                                                            | `App.vue:47`；`stores/fleet.ts:135`                          | 中                                                 |
| 9.12 | **登出不清空 store 中的设备/编队/轨迹**，只断链 → 换账号登录会先看到上一个人的数据                                                                                                                                                                                         | `App.vue:78-82`                                              | 中（Phase 15 一并处理）                            |
| 9.13 | **历史页 `limit` 的 `min/max` 不产生任何约束**（无 `<form>`），越界值由后端 400 转成"加载失败"                                                                                                                                                                             | `HistoryView.vue:187-190`                                    | 低                                                 |
| 9.14 | **history limit 三处口径不一致**：schema 允许 5000、服务端硬夹 500、前端默认请求 1000、输入框上限 5000                                                                                                                                                                     | `validation.ts:15`；`persistence.ts:421`                     | 低                                                 |
| 9.15 | **切换设备下拉不重置回放**：旧样本继续播，而设备名 / trail key / 场景立刻切到新设备                                                                                                                                                                                        | `HistoryView.vue`（无 `watch(deviceId)`）                    | 中                                                 |
| 9.16 | **历史页第 5 个空态分支同时承担"初始未加载"与"有数据但场景定义缺失"** → 后者显示的是"选择设备并加载轨迹"，误导                                                                                                                                                             | `HistoryView.vue:250-253`                                    | 低                                                 |
| 9.17 | **告警桶计数与列表条数不一致**：计数不受筛选影响且含已确认项，而默认列表隐藏已确认                                                                                                                                                                                         | `AlertsView.vue:45-50`                                       | 低                                                 |
| 9.18 | **告警搜索把字面 `"undefined"` 拼进匹配串**（`deviceName` 无 `\|\| ""` 兜底，而 `info`/`detail` 有）                                                                                                                                                                       | `AlertsView.vue:81`                                          | 低                                                 |
| 9.19 | **无时间戳的告警被排到最前**（`toTimestampMs` 回退 `Date.now()`）；采样时间同理会显示"当前时间"而非 `--`                                                                                                                                                                   | `lib/fleetNormalize.ts:61-74`                                | 低                                                 |
| 9.20 | **告警确认按钮无 `aria-pressed`、无 `aria-label`**（严重度桶已经补了，这里漏了）；两个批量操作无 toast、无确认弹窗                                                                                                                                                         | `AlertsView.vue:230-232`                                     | 低                                                 |
| 9.21 | **登录空字段静默不提交、无提示**                                                                                                                                                                                                                                           | `LoginForm.vue:21-26`                                        | 低                                                 |
| 9.22 | **5 个 `route.meta.title` 全部定义、零消费**，页面标题恒为 index.html 的值 → 浏览器标签页与历史记录无法区分页面                                                                                                                                                            | `router/index.ts:22-48`                                      | 低                                                 |
| 9.23 | **token 刷新是 fire-and-forget**，401 不改状态、不登出、不提示                                                                                                                                                                                                             | `useAuth.ts:46-51`                                           | 中（Phase 15 一并处理）                            |
| 9.24 | **无全局 401 拦截**：`fleetApi` 把任何非 2xx 统一抛 `HTTP n`，会话过期表现为"加载失败"而不是跳登录                                                                                                                                                                         | `services/fleetApi.ts:70-72`                                 | 中                                                 |
| 9.25 | **`matchMedia` change 监听注册后永不移除**，也未回退 `addListener`                                                                                                                                                                                                         | `useTheme.ts:76-80`                                          | 低                                                 |
| 9.26 | **`.alert-drawer` / `.drawer-scrim` 是死 CSS**（161 行 partial 仍被 import，标记不存在于任何 `.vue`）                                                                                                                                                                      | `assets/styles/alert-drawer.css`                             | 低                                                 |
| 9.27 | **`extent 描边` 硬编码 `rgba(15,28,39,0.56)` 未主题化**，浅色主题下不协调                                                                                                                                                                                                  | `RosSceneMap.vue:426-435`                                    | 低                                                 |
| 9.28 | **`buildWorldPath` 静默直连缺口**：非有限点被丢弃但其余仍拼接，轨迹断点被画成直线；首点被丢则路径以 `L` 开头                                                                                                                                                               | `RosSceneMap.vue:189-204`                                    | 低                                                 |
| 9.29 | **`RosSceneMap.vue:248-249` 注释失效**（说 `deviceExtentBounds` 供 `effectiveWorldBounds` 用，后者从不引用它）                                                                                                                                                             | 同上                                                         | 低（文档）                                         |
| 9.30 | **`toneLabelMap` 五项字典在两处逐字重复**；`hasPose` 有三份独立实现；场景合并有两份                                                                                                                                                                                        | `DashboardView.vue:36-42`；`GpsMap.vue:20-26` 等             | 低（Phase 12A 一并清）                             |

### 核销结果（Phase 13 收口 · 2026-08-31）

上面那张表写的是 **v1.0.0 的现状**，它是盘点记录，不随新前端改动。下面这张是逐条核对
`frontend-next` 之后的结论 —— **每行给证据位置，而不是给一个勾**。

**已修 19 · 部分 3 · 仍在 7 · 不适用 1 = 30。** 7 条「仍在」每条都写明去处，没有一条是漏掉的；
其中 5 条归入 **13S**（下一个 PR），2 条按原表的判断留 **Phase 15**（会话边界，与 9.12 是同一件事）。

| #    | 结论      | 证据 / 去处                                                                                                                                                                                                                                                                                                                                           |
| ---- | --------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 9.1  | 🔴 仍在   | `formatNumber` 只挡非有限值，而 `Number(null) === 0` **是**有限值 → `formatNumber(null)` 仍渲染 `"0.00"`（`packages/fleet-core/src/formatters.ts:12-18`）；`createDefaultDevice` 仍把 `speed/soc/x/y/yaw` 置 `null`（`fleetNormalize.ts:149-153`）。12A 刻意**按现有行为写测试而不是修它**，所以两套前端都还这么显示 → **13S**                        |
| 9.2  | 🟢 已修   | 位姿每轨道提取一次 + 轨迹增量维护（`composables/useHistoryPlayback.ts`）。该文件头注释同时写明**没有**修掉什么：SVG path 序列化仍是每帧 O(n)，那是画折线固有的                                                                                                                                                                                        |
| 9.3  | 🟡 部分   | `getBoundingClientRect` 降为一次、sessionStorage 写入由 `useSceneViewportPersistence` 合并；**`pointermove` 仍未按 rAF 合帧** → **13S**                                                                                                                                                                                                               |
| 9.4  | 🔴 仍在   | 栅格底图 `<image>` 无 `@error`（`components/map/SceneMap.vue:440-453`），而同组件的点云有 `pointCloudError` 且渲染失败态 —— 不一致照抄了过来 → **13S**                                                                                                                                                                                                |
| 9.5  | 🟡 部分   | `loadPointCloud` 有 `pointCloudRequestId` 守卫（`composables/useSceneOverlay.ts:88-105`），`loadOverlay` / `loadMetadata` 仍无（同文件 `:56` / `:72`）→ **13S**                                                                                                                                                                                       |
| 9.6  | 🟢 已修   | 改法是**取消强制切换**：`selectFormation` 只设编队并重算选中项（`stores/fleet.ts:619-623`），地图模式归用户所有并落 `navfleet:map-mode` —— 口径不一致的根源被去掉了，不是补一次落盘                                                                                                                                                                   |
| 9.7  | 🔴 仍在   | `!sceneReady` 覆盖层 `absolute inset-0` 无 `pointer-events-none`（`SceneMap.vue:652-660`），压在带 `@wheel`/`@pointerdown` 的 svg 上；**同组件的点云失败覆盖层与 GpsMap 都是对的** → **13S**                                                                                                                                                          |
| 9.8  | 🟢 已修   | `data-amap-state` 让「脚本已加载完但 `window.AMap` 缺席」这一格可被回答，`settleFromWindow` 兜底（`lib/amap.ts:9-15,57-104`）                                                                                                                                                                                                                         |
| 9.9  | 🟢 已修   | 全局 `:focus-visible`（`styles/base.css:34-41`）—— 一处规则而不是逐控件补，这也是 13F-2 键盘复核能一次过的原因                                                                                                                                                                                                                                        |
| 9.10 | 🟢 已修   | GPS 图 `role="img"` + `aria-label` 同处（`components/map/GpsMap.vue:347-348`）。ROS 图**保留** `role="img"`：内部标记对 AT 无意义，名字由 `aria-label` 承担，这是设计决定而非漏改                                                                                                                                                                     |
| 9.11 | 🟢 已修   | `bootstrapPending` 分支排在 `apiReady` **之前**（`stores/fleet.ts:705-712`）→ 冷启动说「连接中」而不是「后端离线」。头注释写了理由：正在建立的链路不是正在恢复的链路                                                                                                                                                                                  |
| 9.12 | 🔴 仍在   | 登出只 `fleet.disconnectRealtime()`（`App.vue:55-63`），store 无 reset → **Phase 15**（原表就这么标的，维持）。与 9.24 是同一件事：会话边界。拆开做会把同一处逻辑写两遍                                                                                                                                                                               |
| 9.13 | 🟢 已修   | 没有 `limit` 输入了 —— 一个约束不了任何东西的控件，正确的修法是删掉它而不是给它加 `<form>`；服务端 cap 说话，并**报出实际覆盖跨度**（`components/device/DevicePlaybackTab.vue` 头注释）                                                                                                                                                               |
| 9.14 | 🟢 已修   | 前端不再发 `limit`，四处口径缩成一处（服务端）。这是 9.13 的同一处改动带来的结果                                                                                                                                                                                                                                                                      |
| 9.15 | 🟢 已修   | `watch(() => deviceId)` 先清样本再取新窗口（`DevicePlaybackTab.vue:182-189`）；更根本的是设备已由路由决定，不再有下拉可切                                                                                                                                                                                                                             |
| 9.16 | 🟢 已修   | 五个空态各自独立：加载 / 错误 / 无数据 / 缺 ROS 位姿 / 缺场景定义（`DevicePlaybackTab.vue:422-469`）                                                                                                                                                                                                                                                  |
| 9.17 | 🟢 已修   | 桶计数与列表同源于 `filtered`（`views/AlertsView.vue:139-165,381`）                                                                                                                                                                                                                                                                                   |
| 9.18 | 🟢 已修   | 检索串 `.filter(Boolean)` 后再 join，空字段不再变成字面 `"undefined"`（`AlertsView.vue:148-156`）                                                                                                                                                                                                                                                     |
| 9.19 | 🔴 仍在   | 根因未动：`toTimestampMs` 对空值仍回退 `Date.now()`（`fleetNormalize.ts:69-82`），并经 store 进入排序。**显示面已收窄**：告警行改显 onset（`AlertsView.vue:337`）、采样时间显式挡住（`DevicePlaybackTab.vue:293`）→ 与 9.1 同批 **13S**，它们是同一类错误：**空值伪装成一个看起来像真的值**                                                           |
| 9.20 | 🟢 已修   | 确认按钮 `aria-pressed` + `aria-label`（`AlertsView.vue:353-354`），批量操作有 toast（`:186`）                                                                                                                                                                                                                                                        |
| 9.21 | 🟢 已修   | 空字段给出 `请输入用户名和密码`，并接 `aria-invalid` / `aria-describedby`（`components/LoginForm.vue:44,82-83,105-110`）                                                                                                                                                                                                                              |
| 9.22 | 🟢 已修   | `document.title` 单点消费 `route.meta.title`（`App.vue:89-97`），面包屑复用同一个字段（`components/shell/AppBreadcrumbs.vue:24-26`）—— 定义了却没人读的元数据变成两个读者                                                                                                                                                                             |
| 9.23 | 🟢 已修   | 定时刷新 + 401 → 匿名 + 其它原因退避 + toast（`composables/useAuth.ts:19-56,132-173`）。**原表标的「Phase 15」提前在 12C-1 做掉了**，这里更正                                                                                                                                                                                                         |
| 9.24 | 🔴 仍在   | `fleetApi` 仍把非 2xx 统一抛 `HTTP n`（`packages/fleet-core/src/fleetApi.ts:73-74`），全站只有 `useAuth` 自己比较 401 → **Phase 15**，与 9.12 同一处会话边界                                                                                                                                                                                          |
| 9.25 | 🟢 已修   | `matchMedia` 监听在 scope 结束时移除（`composables/useTheme.ts:76`），头注释点名这是旧实现漏的那一处                                                                                                                                                                                                                                                  |
| 9.26 | ⚪ 不适用 | 死 CSS 属于 v1.0.0 的 partial 体系；新前端没有 CSS partial，Phase 14 旧前端下线时它随之消失。**不需要动作，但要在 Phase 14 确认它确实一起走了**                                                                                                                                                                                                       |
| 9.27 | 🟢 已修   | extent 描边走 token：`.ros-world-frame { stroke: var(--color-map-grid) }`（`SceneMap.vue:688-692`），随主题切换                                                                                                                                                                                                                                       |
| 9.28 | 🔴 仍在   | `buildWorldPath` 仍丢弃非有限点后直接 `join`（`SceneMap.vue:227-235`），**逐字与旧实现相同**：缺口被画成直线；`index === 0` 决定 `M`，首点被丢则整条路径以 `L` 开头 → **13S**                                                                                                                                                                         |
| 9.29 | 🟢 已修   | 那条失效注释没有随搬迁带过来（`SceneMap.vue` 无 `deviceExtentBounds` 之说）                                                                                                                                                                                                                                                                           |
| 9.30 | 🟡 部分   | 那张 tone 文案字典已单源为 `deviceToneLabels`（`packages/fleet-core/src/deviceTone.ts:33-39`，唯一消费点 `DeviceDetailView.vue:106`）；`hasPose` 仍有三份独立实现（`fleetNormalize.ts:97`、`composables/useSvgViewport.ts:93`、`SceneMap.vue:54`）、场景合并仍两份 → **13S** 清理。这是可清理项，不是缺陷 —— 三份实现目前**行为一致**，风险在将来分叉 |

**两处对原表判断的更正**，写在这里而不是悄悄改掉上面那张表：

1. **9.23 不必等 Phase 15** —— 原表标「Phase 15 一并处理」，实际在 12C-1 就做了，因为 token 刷新
   失败必须有人告诉用户，而那时正在建的就是 toast 宿主。
2. **9.6 的修法与原表设想的相反** —— 原表说"强制切 scene 模式但不落盘"，暗示的修法是补一次落盘；
   实际的修法是**去掉强制切换**。用户选的模式凭什么被选编队这个动作改写？口径不一致的根源是那次
   强制，不是那次没落盘。

**「仍在」的分布不是随机的。** 5 条归 13S 的里面有 3 条（9.4 / 9.7 / 9.28）都在 `SceneMap.vue`，
且都是 13A-2a **地图底座搬迁**时被逐字搬过来的 —— 那次 PR 的目标是"不含界面地把地图搬过来"，
所以刻意没有夹带行为改动。这个决定本身是对的（搬迁与修缺陷混在一个 PR 里，出问题时分不清是哪一半），
代价就是这三条要在收口时补。剩下 2 条（9.1 / 9.19）在 `fleet-core`，同样是 12A "抽取不改行为"
的刻意结果 —— 它们的测试当时就是照着**错误行为**写的，所以修的时候要连测试一起改。

## 10. 测试与覆盖现状（新前端的起点）

| 区域             | 现状                                                                                                                                                                                                                                 |
| ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 组件测试         | `AlertsView` **0%** · `HistoryView` **0%** · `useSvgViewport` **1.07%** · `useSceneOverlay` 3.73% · `point-cloud` 1.06% · `utils/amap` 10% · `GpsMap`/`RosSceneMap`/`NotificationHost` 0%（前两个在全部 Dashboard 测试里被 stub 掉） |
| 两个地图         | **完全没有单元测试**。唯一可执行的地图行为规格是 4 个 e2e 用例 + axe                                                                                                                                                                 |
| e2e              | **17 例，零 `data-testid`**，全部 `getByRole` + 中文可访问名匹配 → **只要新前端保持相同语义结构与文案，这 17 例能一字不改复用**，这是等价性最硬的判据                                                                                |
| e2e 的 24px 判据 | 测的是 `.ros-marker.fusion .ros-marker-core` 的盒中心对面板中心，**刻意用核心圆**而不是 marker 组——组的 bbox 含朝向箭头与标签，会报一个恒定的假偏移                                                                                  |
| axe              | 5 页 × 明暗双主题，`wcag2a + wcag2aa`，serious/critical 阻塞，**无 exclude、无 disableRules**；已知边界：单视口 1440×900、单引擎 chromium、只审默认状态、`incomplete` 桶不断言                                                       |

## 11. 数字汇总

| 项                              | 数量                                                               |
| ------------------------------- | ------------------------------------------------------------------ |
| SFC                             | 12 个 / 2,537 行（**全部无 `lang="ts"`**）                         |
| composable                      | 8 个 / 1,404 行                                                    |
| store                           | 1 个 / 761 行 / 28 个导出键                                        |
| 纯逻辑（可直接复用）            | 1,049 行                                                           |
| 依赖 DOM 但无框架耦合（可复用） | 553 行                                                             |
| Vue 耦合但可搬                  | 2,138 行                                                           |
| CSS                             | 22 个 partial / 2,580 行 / 37 个 token                             |
| 路由                            | 5 条                                                               |
| 浏览器存储 key                  | 5 个                                                               |
| toast 触发点                    | 12 处                                                              |
| 枚举字典                        | 3 张 / 13 条（**报码字典不存在**）                                 |
| 本清单条目                      | 约 210 项                                                          |
| 待修缺陷                        | **30 项**（核销见第 9 节末：已修 19 · 部分 3 · 仍在 7 · 不适用 1） |
