# NavFleet 控制台（v3）前端环境变量
#
# 复制为 .env 后填写。**`.env` 已被 .gitignore 忽略，不要提交真实密钥。**
# 这些是 Vite 的构建期变量：改动后需要重启 dev server 或重新构建。

# 高德地图 Web 端 Key 与安全密钥。两个都必须填，否则 GPS 地图会显示配置提示
# 而不是地图（这是刻意的：静默显示一张空地图更难排查）。
# 申请：https://console.amap.com/ → 应用管理 → Web端(JS API)
VITE_AMAP_KEY=
VITE_AMAP_SECURITY_JS_CODE=
